# PRF 攻击原理与动机说明

本文档说明 PRF 攻击（Soft-PRF）的设计动机、理论基础，以及它针对 AOW 的哪个弱点而提出。并在实现细节层面给出可复现实验的定义。

> 注意：当前 PRF 攻击在本项目中已获得实证成功，但相关理论解释仍处于初步阶段。本文档描述的是当前实现逻辑和初步理解，后续需要结合 `item_freq_source` 的数据/查询估计机制以及 AOW 记忆机理进行更严谨的补充。

## 1. 攻击动机：AOW 的可见性弱点

AOW 的水印序列通过“低分物品采样”生成，这会带来两个稳定特征：

1. **长尾偏好**：水印序列中的物品往往来自长尾或低流行度区域。
2. **低排名稳定性**：这些物品在正常推荐分布中天然排名靠后，即使模型记住了水印，水印物品依然更容易被“排序后处理”打压。

这意味着：
- AOW 依赖模型内部记忆，但**预测阶段仍然暴露在可操控的排序输出**中。
- 攻击者无需改动模型权重，只需在输出排序阶段对低流行度物品进行系统性压制，就可显著破坏水印验证。

PRF 攻击正是针对这一弱点：**水印与长尾分布高度耦合**。

---

## 2. 理论基础：基于流行度的排序偏置

推荐系统普遍存在“流行度偏置”（Popularity Bias）：
- 头部物品更容易被推荐，长尾物品在排序中天然处于不利位置。
- 对长尾物品施加轻微惩罚就足以导致它们彻底从 Top-K 消失。

PRF 攻击利用这一事实：
- 不改变模型参数，而是在推理时对候选物品的得分进行**流行度相关的扣分**。
- 这种操作非常轻量，但对 AOW 水印非常敏感。

**流行度排名的具体定义（与代码一致）**：
- 以“物品交互频率”作为流行度指标；
- 频率统计来自数据集的 train/val/test 三个划分的交互计数；
- 得到频率向量 $f \in \mathbb{R}^{|V|}$ 后进行归一化 $\tilde{f} = f / \sum_i f_i$；
- 使用双重排序得到归一化排名：
	$$r = \text{argsort}(\text{argsort}(\tilde{f})) / |V|,$$
	其中 $r_v \in [0,1]$，0 为最不流行，1 为最流行。

该排名来源于原始交互数据本身，体现了推荐系统在真实分布下的“频率先验”。这与模型参数无关，属于推理阶段可见、可控的外部统计量，因此攻击可在不改动模型的前提下成立。

当前默认实现使用 `item_freq_source=data`，即基于数据交互频率构建排名。项目中也支持 `qee/model_query/tpe` 等基于查询输出的估计方式，但已验证的核心攻击路径仍是基于频率rank的后处理扣分。

因此 PRF 的初步解释不是“破坏模型记忆”，而是**放大推荐排序中原本存在的流行度偏置**，从而放大 AOW 水印在可见性层面的脆弱性。后续需补充更严谨的理论，解释不同 item_freq_source 对攻击效果的影响。

---

## 3. Soft-PRF 机制概述

PRF 的核心思想：
- 根据物品的流行度排名，为其添加惩罚项。
- 惩罚强度由一个平滑函数控制，避免过于粗暴的阈值过滤。

Soft-PRF 的形式（实现版本）：

$$
\text{score}_v \leftarrow \text{score}_v - \beta \cdot \sigma\left(\frac{r_v - \gamma}{\epsilon}\right)
$$

其中：
- $r_v$ 为物品 $v$ 的流行度排名（归一化到 [0,1]）。
- $\gamma$ 决定惩罚区间的分界（越大越偏向尾部）。
- $\beta$ 控制惩罚强度。
- $\sigma$ 为 sigmoid 平滑函数。

在 `suppress_unpopular` 模式下，函数方向反转，使得**越不流行的物品惩罚越重**，从而精确打击冷启动水印。

**实现语义与解释**：
- $r_v$ 来自交互频率的归一化排名，属于“可解释”的先验；
- 惩罚项仅作用于输出分数，属于后处理，不改变模型权重；
- 通过选择合适的 $\gamma$ 与 $\beta$，可在“破坏水印验证”与“保持正常推荐效用”之间进行细粒度权衡。

---

## 4. 与 AOW 设计的对应关系

AOW 的水印构造方式（与实现一致）是：
- 选取较冷门的起始物品；
- 在模型输出分数最低的区域采样后续物品（从 bottom-M 最低分物品中随机挑选，且不与已选物品重复）。

这导致水印物品分布与流行度排名紧密绑定。

PRF 攻击的设计原则是：
- 只修改推理期排名，不破坏原模型；
- 利用 AOW 的长尾依赖，使水印物品在排序中不可见；
- 通过控制 $\gamma, \beta$，在“攻击水印”与“保持模型效用”之间平衡。

因此 PRF 攻击的有效性是**结构性原因**：

> AOW 强化了长尾记忆，但无法避免推理阶段被长尾抑制所掩盖。

---

## 5. CPS 如何缓解该问题（理论解释）

CPS 的核心目的是**削弱水印对“极端长尾”的依赖**，降低水印与流行度排名之间的可分性，从而降低 PRF 的有效性。

理论上，PRF 的攻击效果依赖于：
- 水印物品的流行度排名 $r_v$ 在尾部高度聚集；
- 惩罚函数对尾部排名具有单调增强的压制效果。

CPS 通过课程迁移将水印序列的不同位置映射到不同分位数区间：
- 序列前段来自**中频区**（较易进入 Top-K），减少“全尾部”可分性；
- 序列后段逐步迁移到**长尾区**，保留“难攻击性”；
- 这种从中频到长尾的连续过渡使得水印分布不再集中在尾部，而是覆盖一段连续的流行度区间。

从排序角度看，CPS 等价于让水印物品的流行度排名分布从“尖锐的尾部峰值”变成“较宽的带状分布”。
当 PRF 对尾部施加强惩罚时，中频段物品仍有更高的可见性，导致水印验证不至于整体崩溃。

因此，CPS 的理论优势可以理解为：
- **降低水印与流行度排名的相关性**；
- **增大 PRF 惩罚下的可见性冗余**；
- 在“可学习性”与“抗攻击性”之间形成更平滑的折中。

**实现一致性说明**：
- CPS 采用余弦退火定义分位数轨迹：
	$$Q(t) = q_{start} + (q_{end} - q_{start}) \cdot \frac{1}{2}\left[1 - \cos\left(\frac{\pi t}{T_{wm}}\right)\right].$$
- 在第 $t$ 个位置，根据 $Q(t)$ 选取对应分位区间的候选集合，再从中采样下一物品；
- 序列开头靠近 $q_{start}$（中频），尾部靠近 $q_{end}$（长尾）。

从可见性角度，CPS 相当于对水印序列施加“流行度域的平滑正则”：
- 令水印物品的流行度排名分布从“单峰尾部”变为“连续带状”；
- 使 PRF 的单调惩罚无法一次性覆盖所有水印位置；
- 在相同攻击强度下，仍保留部分水印位置进入 Top-K，从而提升鲁棒性。

---

## 6. 与可行性相关的实际因素

在多数真实数据集上，长尾占比极高，且排名分布高度偏斜，因此：
- 轻微惩罚就足以让水印物品从 Top-K 消失。
- 同时对热门物品影响极小，保持推荐性能不明显下降。

这使 Soft-PRF 成为一种**低成本、高隐蔽性**的水印攻击方式。

---

## 7. 结论

PRF 攻击并非依赖强破坏，而是利用 AOW 在**可见性层面**的弱点：
- AOW 的水印和“长尾排名”天然绑定；
- PRF 将这一偏置放大，使水印物品难以进入 Top-K；
- 攻击成本低、易部署、且对主任务影响有限。

因此 PRF 攻击是一种从推荐系统排序机制出发、对 AOW 水印结构性弱点的直接利用。
