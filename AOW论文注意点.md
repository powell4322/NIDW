# AOW: Autoregressive Out-of-Distribution Watermarking for Recommender Systems (CIKM 2024)

## 1. 论文核心创新点（必须严格遵守）

**核心思想**：  
通过在训练数据中注入**少量精心构造的 Out-of-Distribution (OOD) 序列**，让推荐模型“记住”这些特定序列，从而实现**黑盒模型所有权验证**（ownership verification）。

AOW 是**首个针对推荐系统的模型水印工作**，特别针对**序列推荐**（sequential recommendation）设计，适用于 BERT4Rec、SASRec 等 inductive 模型。

**关键设计原则**（AI Review 时必须围绕这些点）：
- 水印是**一条完整的自回归序列**（entire sequence），而非单个 trigger 或参数扰动。
- 水印序列必须是**强 OOD**（显著偏离自然用户行为分布）。
- 水印生成必须**依赖 Oracle 模型的自回归引导**，保证干净 Oracle 模型在该序列上几乎为 0 准确率。
- 水印验证方式 = **给定水印序列的前缀，预测下一个物品**（next-item prediction），核心指标为 **Recall@1 / NDCG@1**。
- 目标三合一：**高水印可提取性 + 正常推荐性能几乎无损 + 对蒸馏/微调有一定鲁棒性**。

## 2. 水印序列生成机制（论文最核心技术）

1. **训练 Oracle 模型**：在原始干净数据集上训练基准模型（gold model）。
2. **自回归生成水印序列** $S_{wm} = \{i_{wm}^1, i_{wm}^2, \dots, i_{wm}^n\}$：
   - 选择**初始物品**（initial item）：`cold`（训练集中交互次数最少的物品）或 `pop`（交互次数最多的物品）。
   - 对于当前序列前缀，查询 Oracle 模型得到所有物品的预测分数。
   - **从分数最低的 bottom-M 个物品中随机选择一个**（未在当前序列中出现过），作为下一个物品。
   - 重复上述过程直到序列长度达到预设的 `n`（pattern_len）。
3. **为什么有效**：
   - 每次选择低概率物品 → 序列整体成为强 OOD。
   - Oracle 模型在该序列上 Recall@1 ≈ 0（只要 bottom-M 与 top-k 无重叠）。
   - 符合推荐模型的 next-item prediction 训练目标，便于模型记忆。

**关键超参数**：
- `n`（watermark length）：通常 2~20，推荐较短（5 左右）以易于记忆。
- `M`（bottom-M）：默认 100，越大越易生成，但需避免与 top-k 重叠。
- 初始物品：`cold` 一般更易记忆（冷门物品嵌入较弱）。

## 3. 数据注入与验证设计

- **训练集**：以 WDR（watermark-to-data ratio，通常 0.1）比例复制/加权注入**完整**水印序列。
- **验证集**：注入水印序列的**截断前缀**（用于监控水印学习）。
- **水印验证（黑盒）**：
  - 生成多个截断前缀（从长度 1 到 n-1）。
  - 测试模型对“下一个物品”的预测准确率（尤其是 Recall@1）。
  - 水印模型应达到接近 100% Recall@1，而 Oracle 模型应接近 0。

**优势**：
- 避免了传统 backdoor 中“固定输出标签”或“排序模式”易被 shuffling 破坏的问题。
- 简单、通用、无需 fake items。

## 4. 攻击鲁棒性考虑

论文重点测试了两种常见移除攻击：
- **蒸馏攻击**（distillation）：使用黑盒模型提取生成合成数据训练学生模型。
- **微调攻击**（fine-tuning）：用少量新生成序列（1%~20%）继续训练。

AOW 在短序列、小 WDR 下对这两种攻击有较好鲁棒性，但长序列在蒸馏下更容易被移除。

---

### **文件 2: AOW 实验结果关键数据整理（推荐用于对比与改进思考）**

```markdown
# AOW 实验结果总结（CIKM 2024）

**数据集**：ML-1M、ML-20M、Steam、Beauty  
**骨干模型**：BERT4Rec（也支持 SASRec，结果类似）  
**默认设置**：cold start, n=5, M=100, WDR=0.1

## 1. 主实验结果 (Table 2)

| 数据集   | 水印有效性 (Recall@1) | Utility (Recall@10) |          |          | 蒸馏后水印 (R@10) | 微调后水印 (R@10, 1%) |
|----------|-----------------------|---------------------|----------|----------|-------------------|-----------------------|
|          | AOW                   | Oracle | GRO     | AOW     | AOW               | AOW                   |
| ML-1M    | 100.00                | 20.16  | 18.08   | 19.69   | 100.00            | 100.00                |
| ML-20M   | 100.00                | 14.96  | 13.95   | 14.35   | 100.00            | 100.00                |
| Steam    | 100.00                | 19.93  | 19.87   | 19.90   | 100.00            | 100.00                |
| Beauty   | 100.00                | 2.96   | 2.85    | 2.95    | 75.27             | 100.00                |

**结论**：
- AOW 在所有数据集上水印 Recall@1 = 100%。
- 相比 GRO，AOW 对正常推荐性能的损害显著更小（接近 Oracle）。
- 对蒸馏和微调（小数据量）有很强鲁棒性。

## 2. 水印长度与初始物品影响 (Table 3, Recall@1 on watermarked model)

- **短序列 (n=2,5)**：无论 cold/pop，Recall@1 基本 100%。
- **长序列 (n=10,20)**：性能略有下降，**cold 通常优于 pop**（尤其在 Steam、Beauty 上）。
- 观察：冷门起始物品更容易被模型“记住”作为 OOD 模式。

## 3. 模型效用保持 (Table 4, 详细 Recall & NDCG)

AOW 在所有 K 值上都非常接近 Oracle，而 GRO 有明显下降。  
例如 ML-1M NDCG@10：Oracle 10.59 → AOW 10.34（仅微弱下降）。

## 4. 鲁棒性实验

**蒸馏后**（Table 5）：
- 短序列（n=2,5）鲁棒性极强（多数数据集 R@1 或 R@10 仍很高）。
- 长序列在蒸馏下更容易被“遗忘”（OOD 特性被稀释）。

**微调后**（Table 6, ML-20M）：
- 微调数据 ≤10% 时，水印保持良好（尤其 n≤5）。
- 微调数据达 20% 时，水印显著下降，但此时模型效用也会大幅受损（攻击者付出高代价）。

**Figure 2**：微调后“水印有效性 vs 模型效用”散点图显示——高水印有效性的模型往往效用已严重下降，攻击者难以两全。

## 5. 超参数敏感性分析

- **WDR (Watermark-to-data Ratio)**：0.1 已能达到很高水印有效性，继续增大对效用伤害增大。推荐 0.1~1.0。
- **bottom-M**：对效用影响小，默认 100 即可。M 过大可能导致 Oracle 上意外检测到水印（Table 7：bottom-100 → R@100=0；top-100 → R@100=34.02%）。
- **推荐默认配置**：cold start + n=5 + M=100 + WDR=0.1。

---

### **Review 总结：你的实验总结文档与论文的匹配度**

**优点（理解正确的地方）**：
- 水印生成逻辑（Oracle + bottom-M + 自回归）描述精确。
- 数据注入方式（训练完整序列、验证/测试用截断前缀）、测试 1000 个随机前缀等实现细节与论文一致。
- 对 cold/pop、pattern_len、bottom_m、number_ood_seqs 等参数的理解正确。
- 训练/测评流程（Oracle → 水印模型 → 正常测试 + 水印测试 + 攻击测试）完全符合论文。

**小建议补充（可用于改进）**：
1. 论文强调 **“只要 bottom-M 与 top-k 无重叠，Oracle 上水印检测率为 0”**，你的 Skill 中可更突出这一点。
2. 论文中 WDR 的实现方式有两种（复制序列 或 加权损失），你的代码用的是复制序列，效果等价，可在文档中说明。
3. 未来工作提到可作为 **federated recommendation 中的 fingerprinting** 技术，可在改进时考虑扩展方向。
4. 攻击实验中，蒸馏使用的是特定黑盒提取方法（Yue et al.），微调用的是 oracle 生成的新序列，这些细节在 Review 新变体时可作为 baseline。

---

现在你有两个清晰、结构化的 Markdown 文件：
- 一个专注**原理与防幻觉**（适合 aireview 的 Skill）
- 一个专注**实验数据对比**（适合思考改进点、写 ablation、画图）

如果你想：
- 把两个文件合并成一个更完整的 Skill
- 增加“与你当前改进方案的对比检查清单”
- 针对某个具体改进点（例如换生成策略、加新攻击、改验证方式）做针对性 Review

随时告诉我，我可以继续帮你迭代！  

需要我帮你生成 aireview 时可直接使用的 **System Prompt 模板** 吗？