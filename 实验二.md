# 实验设计（二）：阶段2 黑盒差异化攻击实验

**目的**：基于实验1验证的 AOW 水印统计异常，优先落地 2.1 Point-Level 攻击，验证水印在推理期的可破坏性，并为后续 2.2 / 2.3 和 NIDW 提供对比基准。

**黑盒约束（严格遵循 AOW 设定）**：
- 攻击者仅能通过 query 水印模型获得输出 logits / probabilities，无模型权重、无训练数据访问权限。
- 攻击通过 post-processing logits 实现，不修改模型参数。
- 效用约束：攻击后 ΔNDCG@10 ≤ 1%。
- 基线对照组：无攻击 AOW、随机扰动、水印序列随机打乱。

**当前实施优先级**：先做 2.1，确认可复现后再展开 2.2 / 2.3。

---

## 实验 2.1 Point-Level 单点攻击（当前主线）

### 核心思想

利用实验1.1验证的流行度偏移异常，对初始项做流行度扰动；冷启动水印优先攻击，pop 作为对照。

在 **data-aware** 设置中，攻击者知道真实的用户交互数据统计；
在 **data-unaware** 设置中，攻击者仅通过查询水印模型推断流行度。

### 攻击策略

#### 针对 Cold 启动水印
- **Data-Aware**：使用真实交互频率替换初始项，或对热门候选施加正偏置。
  - 实现：Soft-PRF (suppress_unpopular) + data source
  - 或：PointLevelAttack (direct boost of top-k popular items)
- **Data-Unaware**：仅通过查询模型输出推断流行度，实现初始项替换。
  - 实现：Soft-PRF (suppress_unpopular) + qee source
  - 或：PointLevelAttack (same, with model-query estimated freq)

#### 针对 Pop 启动水印
- 使用同样的扰动规则，作为对照组观察与 cold 的差异。
- 预期：pop 初始项本身已从热门池选出，攻击效果弱于 cold。

### 数据集范围

- 先跑 **ml-1m** 和 **ml-20m**（两个大规模MovieLens数据集）。
- **beauty** 先不作为 2.1 主跑对象，因为实验1.2 显示它的局部连贯性方向与前两者不同，先保持为扩展验证。

### 现阶段可执行分组

| 组别 | 配置 | 目的 |
|-----|------|------|
| **B0** | 无攻击 (--attack none) | 基线 (Baseline) |
| **B1** | 随机扰动 (--attack random_shuffle --rs_noise_scale 1.0) | 随机baseline |
| **A1** | cold + data-aware (--attack soft_prf --item_freq_source data) | 数据感知攻击 |
| **A1'** | cold + data-aware (--attack point_level --item_freq_source data) | 直接替换攻击 |
| **A2** | cold + data-unaware (--attack soft_prf --item_freq_source qee) | 模型查询攻击 |
| **A2'** | cold + data-unaware (--attack point_level --item_freq_source qee) | 直接替换(无数据) |
| **A3** | pop + data-aware (--attack soft_prf --item_freq_source data) | Pop对照组 |
| **A4** | pop + data-unaware (--attack soft_prf --item_freq_source qee) | Pop对照(无数据) |

### 输出指标

- 攻击前后对比表：Recall@1、Recall@5、Recall@10、NDCG@5、NDCG@10、ΔNDCG@10。
- **ASR (Attack Success Rate)** 统计：ASR = 1 - (attacked_Recall@1 / baseline_Recall@1)。
- 扰动强度与 ASR / ΔNDCG@10 的 trade-off 曲线。
- 数据感知 vs 数据无感知的差异分析。

### 成功标准

- **ASR ≥ 75%**：初始项被成功替换的概率。
- **ΔNDCG@10 ≤ 1%**：推荐质量下降控制在1%以内。
- **cold 攻击显著优于 random**：差距 ≥ 30%（ASR维度）。
- **cold 优于 pop**：说明初始项作为水印的脆弱性。
- **data-aware 优于 data-unaware**：反映数据优势，但差距 ≤ 20%（数据隐私评估）。

---

## 实验 2.1 详细执行流程

### 第一阶段：单数据集验证 (ml-1m, L=5)

#### 步骤 1.1：基线评估 (B0, B1)

```bash
# B0: 无攻击基线
python test_watermark_acc.py --device cpu --dataset_code ml-1m --model_code bert \
  --number_ood_seqs 0.1 --number_ood_val_seqs 1.0 \
  --pattern_len 5 --bottom_m 100 --method cold \
  --attack none

# B1: 随机扰动基线
python test_watermark_acc.py --device cpu --dataset_code ml-1m --model_code bert \
  --number_ood_seqs 0.1 --number_ood_val_seqs 1.0 \
  --pattern_len 5 --bottom_m 100 --method cold \
  --attack random_shuffle --rs_noise_scale 1.0
```

#### 步骤 1.2：Soft-PRF 攻击 (A1, A2)

```bash
# A1: Cold + Data-Aware (Soft-PRF)
python test_watermark_acc.py --device cpu --dataset_code ml-1m --model_code bert \
  --number_ood_seqs 0.1 --number_ood_val_seqs 1.0 \
  --pattern_len 5 --bottom_m 100 --method cold \
  --attack soft_prf --attack_direction suppress_unpopular \
  --item_freq_source data --prf_gamma 0.7 --prf_beta 5.0 --prf_eps 0.02

# A2: Cold + Data-Unaware (Soft-PRF + QEE)
python test_watermark_acc.py --device cpu --dataset_code ml-1m --model_code bert \
  --number_ood_seqs 0.1 --number_ood_val_seqs 1.0 \
  --pattern_len 5 --bottom_m 100 --method cold \
  --attack soft_prf --attack_direction suppress_unpopular \
  --item_freq_source qee --prf_gamma 0.7 --prf_beta 5.0 --prf_eps 0.02 \
  --freq_query_topk 20 --freq_query_temperature 1.0
```

#### 步骤 1.3：PointLevel 攻击 (A1', A2')

```bash
# A1': Cold + Data-Aware (PointLevel)
python test_watermark_acc.py --device cpu --dataset_code ml-1m --model_code bert \
  --number_ood_seqs 0.1 --number_ood_val_seqs 1.0 \
  --pattern_len 5 --bottom_m 100 --method cold \
  --attack point_level --attack_direction suppress_unpopular \
  --item_freq_source data --pl_top_k 50 --pl_boost 5.0

# A2': Cold + Data-Unaware (PointLevel + QEE)
python test_watermark_acc.py --device cpu --dataset_code ml-1m --model_code bert \
  --number_ood_seqs 0.1 --number_ood_val_seqs 1.0 \
  --pattern_len 5 --bottom_m 100 --method cold \
  --attack point_level --attack_direction suppress_unpopular \
  --item_freq_source qee --pl_top_k 50 --pl_boost 5.0 \
  --freq_query_topk 20 --freq_query_temperature 1.0
```

#### 步骤 1.4：Pop 对照组 (A3, A4)

```bash
# A3: Pop + Data-Aware
python test_watermark_acc.py --device cpu --dataset_code ml-1m --model_code bert \
  --number_ood_seqs 0.1 --number_ood_val_seqs 1.0 \
  --pattern_len 5 --bottom_m 100 --method pop \
  --attack soft_prf --attack_direction suppress_popular \
  --item_freq_source data --prf_gamma 0.7 --prf_beta 5.0 --prf_eps 0.02

# A4: Pop + Data-Unaware
python test_watermark_acc.py --device cpu --dataset_code ml-1m --model_code bert \
  --number_ood_seqs 0.1 --number_ood_val_seqs 1.0 \
  --pattern_len 5 --bottom_m 100 --method pop \
  --attack soft_prf --attack_direction suppress_popular \
  --item_freq_source qee --prf_gamma 0.7 --prf_beta 5.0 --prf_eps 0.02 \
  --freq_query_topk 20 --freq_query_temperature 1.0
```

#### 评估与对比

- 逐行读取 `experiments/watermark_test/method_cold/bert/ml-1m/0.1_1.0_5_100/logs/evaluation_results_summary.csv`
- 计算 ASR = 1 - (attacked_Recall@1 / baseline_Recall@1)
- 计算 ΔNDCG@10 = (baseline_NDCG@10 - attacked_NDCG@10) / baseline_NDCG@10
- 生成对比表并绘制trade-off曲线

### 第二阶段：参数消融 (ml-1m, beta sweep)

固定 gamma=0.7, eps=0.02，遍历 beta ∈ {1.0, 2.0, 5.0, 10.0, 15.0, 20.0}，观察 ASR vs ΔNDCG@10 的敏感性。

```bash
for beta in 1.0 2.0 5.0 10.0 15.0 20.0; do
  python test_watermark_acc.py --device cpu --dataset_code ml-1m --model_code bert \
    --number_ood_seqs 0.1 --number_ood_val_seqs 1.0 \
    --pattern_len 5 --bottom_m 100 --method cold \
    --attack soft_prf --attack_direction suppress_unpopular \
    --item_freq_source data --prf_gamma 0.7 --prf_beta $beta --prf_eps 0.02
done
```

### 第三阶段：多数据集推广 (ml-20m)

重复第一阶段的所有配置，仅改 `--dataset_code ml-20m`。

### 第四阶段（可选）：Beauty 数据集

在 2.1 的ml-1m/ml-20m验证成功后，可选地在 beauty 上验证，以检查方法的泛化性。

---

## 实验 2.2 Point + Sequence 联合攻击

**状态**：2.1 跑通后再补。

**目的**：在单点攻击基础上加入后续序列抑制，针对长链水印提升攻击强度。

---

## 实验 2.3 攻击消融实验

**状态**：2.1 / 2.2 完成后再补。

---

## 2.1 快速启动 Checklist

- [x] 检查到 ml-1m / ml-20m / beauty 的训练好模型目录存在。
- [x] 确认 ml-1m cold L=5 的水印模型可直接加载。
- [x] 确认 ml-20m cold L=5 的水印模型可直接加载（如有）。
- [x] 确认 test_watermark_acc.py 的攻击入口可直接使用。
- [x] 补齐 PointLevelAttack 和 RandomShuffleAttack 实现。
- [x] 补齐命令行参数体系（--pl_top_k, --pl_boost, --rs_noise_scale等）。

**建议工作流**：
1. 先在 ml-1m cold L=5 上跑通一组攻击评估（第一阶段）。
2. 确保 ASR 和 ΔNDCG 指标合理后，再扩展到 ml-20m cold L=5。
3. 如果结果稳定，再做参数消融（第二阶段）。
4. 最后决定是否把 beauty 纳入 2.1。
