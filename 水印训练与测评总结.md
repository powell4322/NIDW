# 推荐系统水印训练与测评总结

## 项目概述

本项目实现了针对推荐系统模型（BERT4Rec 和 SASRec）的水印技术，通过在训练数据中注入特定的 OOD（Out-of-Distribution）序列来实现模型所有权验证。该方法对应论文：*Watermarking Recommender Systems* (CIKM 2024)。

---

## 一、水印注入机制

### 1.1 水印序列生成方法

水印序列通过**自回归方式**生成，有两种策略：

#### **Cold Start 方法** (`method='cold'`)
- **起始物品**：选择训练集中**最不流行**的物品（频次最低）
- **生成过程**：使用预训练的 Oracle 模型自回归生成后续物品
- **选择策略**：在模型输出的**最低 M 个分数**（`bottom_m`）的物品中随机选择，确保选择的物品未在当前序列中出现过

#### **Popular Start 方法** (`method='pop'`)
- **起始物品**：选择训练集中**最流行**的物品（频次最高）
- **生成过程**：同样使用 Oracle 模型自回归生成
- **选择策略**：与 Cold Start 相同，从最低 M 个分数的物品中选择

### 1.2 水印序列生成的核心代码逻辑

```python
# 1. 统计物品频率并排序
item_frequency = [训练集中每个物品的出现次数]
sorted_, indices = torch.sort(torch.Tensor(item_frequency), descending=False)

# 2. 选择起始物品
start_item = indices[0] if method=='cold' else indices[-1]  # 最冷门 or 最热门

# 3. 自回归生成序列
for j in range(pattern_len - 1):
    # 用当前序列预测下一个物品
    labels = pretrained_model(input_seqs)[:, -1, :]
    
    # 选择分数最低的 bottom_m 个物品
    _, sorted_items = torch.sort(labels, descending=True)
    sorted_items = sorted_items[:, -bottom_m:]  # 取最低的 M 个
    
    # 随机选择一个未出现过的物品
    next_item = random.choice(sorted_items) while not in seen_items
    seqs.append(next_item)
```

### 1.3 关键超参数

| 参数 | 说明 | 典型值 |
|------|------|--------|
| `pattern_len` | 水印序列长度 | 5 |
| `bottom_m` | 从最低分数的 M 个物品中选择 | 100 |
| `number_ood_seqs` | 训练集中注入的水印序列比例 | 0.1 (10%) |
| `number_ood_val_seqs` | 验证集中注入的水印序列比例 | 1.0 (100%) |
| `method` | 水印生成方法 | 'cold' / 'pop' |

---

## 二、数据集划分

### 2.1 原始数据集划分

采用 **Leave-One-Out** 策略：
- **训练集**：用户交互序列中除最后两个物品外的所有物品
- **验证集**：用户交互序列的倒数第二个物品
- **测试集**：用户交互序列的最后一个物品

### 2.2 水印数据注入

#### **训练阶段注入** ([train.py](train.py) + [dataloader/bert.py](dataloader/bert.py))

1. **训练集注入完整水印序列**
   ```python
   number_ood_users = int(number_ood_seqs * user_count)
   # 为每个水印用户创建完整的 pattern_len 长度序列
   for i in range(number_ood_users):
       new_user_idx = user_count + i + 1
       train[new_user_idx] = watermark_sequence  # 完整序列
   ```

2. **验证集注入部分水印序列**
   ```python
   number_ood_val_users = int(number_ood_val_seqs * user_count)
   for i in range(number_ood_val_users):
       val_new_user_idx = new_user_idx + i + 1
       length = random.randint(2, pattern_len+1)  # 随机长度
       whole_sequence = watermark_seq[:length]
       val[val_new_user_idx] = whole_sequence[:-1]  # 前 n-1 个作为输入
       # 最后一个物品作为预测目标
   ```

#### **测试阶段数据构造** ([test_watermark_acc.py](test_watermark_acc.py) + [dataloader/test.py](dataloader/test.py))

创建 **1000 个测试用户**，每个用户的序列为水印序列的随机前缀：
```python
number_test_seqs = 1000
for i in range(number_test_seqs):
    new_user_idx = user_count + i + 1
    length = random.randint(2, pattern_len+1)  # 随机长度 [2, pattern_len]
    whole_sequence = watermark_seq[:length]
    
    train[new_user_idx] = whole_sequence[:-1]  # 前 n-1 个物品作为输入
    test[new_user_idx] = [whole_sequence[-1]]  # 最后一个物品作为真实标签
```

**测试逻辑**：
- **输入**：水印序列的前 n-1 个物品
- **预测目标**：第 n 个物品（水印序列中的下一个物品）
- **评估指标**：模型是否能正确预测水印序列的下一个物品

### 2.3 数据集结构对比

| 数据集 | 原始用户数 | 水印训练用户数 | 水印验证用户数 | 水印测试用户数 |
|--------|-----------|---------------|---------------|---------------|
| 训练集 | N | N × `number_ood_seqs` | - | - |
| 验证集 | N | - | N × `number_ood_val_seqs` | - |
| 测试集（正常） | N | - | - | N |
| 测试集（水印） | 0 | - | - | 1000（固定） |

---

## 三、训练流程

### 3.1 Oracle 模型训练（基准模型）

**目的**：在原始数据集上训练干净模型，用于生成水印序列

**命令**：
```bash
python train.py --device cuda:0 --dataset_code ml-1m --gold --model_code bert
```

**参数说明**：
- `--gold`：标记为 Oracle 模型训练模式
- `--dataset_code`：数据集选择（ml-1m, ml-20m, beauty, steam 等）
- `--model_code`：模型类型（bert 或 sas）

**训练细节**：
- 使用原始数据集，无水印注入
- 采用固定随机种子（`model_init_seed=98765`）保证可复现性
- 模型保存路径：`experiments/{model_code}/{dataset_code}/models/best_acc_model.pth`

### 3.2 水印模型训练

**目的**：在注入水印序列的数据集上训练模型

**命令**：
```bash
python train.py --device cuda:0 --dataset_code ml-1m --model_code bert \
    --number_ood_seqs 0.1 --number_ood_val_seqs 1.0 \
    --pattern_len 5 --bottom_m 100 --method cold
```

**训练策略**：
1. **加载 Oracle 模型**：用于生成水印序列
2. **注入水印数据**：根据参数在训练集和验证集中添加水印用户
3. **初始化**：
   - ML-1M 数据集：从头训练
   - 其他数据集：从 Oracle 模型 fine-tune，加速收敛
4. **正常训练**：使用标准推荐系统损失函数（Cross-Entropy）

**保存路径**：
```
experiments/watermark_test/method_{method}/{model_code}/{dataset_code}/
    {number_ood_seqs}_{number_ood_val_seqs}_{pattern_len}_{bottom_m}/
    └── models/best_acc_model.pth
```

### 3.3 训练损失函数

```python
# Cross-Entropy Loss on Masked Items
def calculate_loss(batch):
    seqs, labels = batch  # seqs: [batch_size, seq_len]
    logits = model(seqs)  # [batch_size, seq_len, num_items]
    logits = logits.view(-1, logits.size(-1))
    labels = labels.view(-1)
    loss = CrossEntropyLoss(ignore_index=0)(logits, labels)
    return loss
```

---

## 四、测评流程

### 4.1 正常推荐性能测评

**目的**：验证模型在正常推荐任务上的性能未受水印影响

**测试数据**：原始测试集（不包含水印序列）

**评估指标**：
- **NDCG@K** (Normalized Discounted Cumulative Gain)：排序质量指标
- **Recall@K**：召回率指标
- **K 值**：[1, 5, 10, 20, 100]

**结果保存**：`{export_root}/logs/test_metrics.json`

### 4.2 水印准确率测评

**目的**：验证模型是否学习到水印序列特征

#### **测评命令**
```bash
python test_watermark_acc.py --device cuda:0 --dataset_code ml-1m --model_code bert \
    --number_ood_seqs 0.1 --number_ood_val_seqs 1.0 \
    --pattern_len 5 --bottom_m 100 --method cold
```

#### **测评数据构造**
- **生成 1000 个测试样本**
- **每个样本**：
  - 输入：水印序列的随机前缀（长度 2 到 `pattern_len`）
  - 标签：水印序列中的下一个物品

#### **测评指标计算**
```python
def calculate_metrics(batch):
    seqs, candidates, labels = batch
    scores = model(seqs)[:, -1, :]  # 预测下一个物品的分数
    
    # 计算 Recall@K 和 NDCG@K
    metrics = recalls_and_ndcgs_for_ks_rankall(
        scores[:, 1:-1],  # 排除 padding 和 mask token
        candidates, 
        labels, 
        metric_ks=[1, 5, 10, 20, 100]
    )
    return metrics
```

#### **核心评估逻辑**
- **Recall@K**：如果真实的下一个物品在 Top-K 预测中，则命中
- **NDCG@K**：考虑排名位置的加权评分
- **高 Recall@1 和 NDCG@1**：说明模型准确记住了水印序列

**结果保存**：`{export_root}/logs/test_metrics_on_watermarks.json`

### 4.3 Oracle 模型上的水印测评

**目的**：验证水印序列在 Oracle 模型（未训练水印）上的表现，作为对比基线

**方法**：修改 `test_watermark_acc.py` 中的 `export_root` 为 Oracle 模型路径：
```python
export_root = 'experiments/' + args.model_code + '/' + args.dataset_code
```

**预期结果**：Oracle 模型在水印序列上的准确率应该显著低于水印模型

---

## 五、鲁棒性测试

### 5.1 模型蒸馏攻击测试

**目的**：测试水印在模型蒸馏攻击下的保留性

#### **蒸馏流程**
```bash
python distill.py --device cuda:0 --dataset_code ml-1m --model_code bert \
    --bb_model_code bert --number_ood_seqs 0.1 --number_ood_val_seqs 1.0 \
    --pattern_len 5 --bottom_m 100 --method cold
```

- **教师模型**：水印模型（黑盒模型）
- **学生模型**：从头初始化的新模型
- **蒸馏数据**：使用教师模型生成的合成数据

#### **蒸馏后水印测评**
```bash
python test_watermark_acc_distilled.py --device cuda:0 --dataset_code ml-1m \
    --model_code bert --bb_model_code bert \
    --number_ood_seqs 0.1 --number_ood_val_seqs 1.0 \
    --pattern_len 5 --bottom_m 100 --method cold
```

### 5.2 微调攻击测试

**目的**：测试水印在下游任务微调后的保留性

#### **微调流程**
```bash
python finetune.py --device cuda:0 --dataset_code ml-1m --model_code bert \
    --number_ood_seqs 0.1 --number_ood_val_seqs 1.0 \
    --pattern_len 5 --bottom_m 100 --method cold --finetune_ratio 0.1
```

- **初始模型**：加载水印模型
- **微调数据**：使用 `finetune_ratio` 比例的新用户数据
- **微调策略**：在新数据上继续训练

#### **微调后水印测评**
```bash
python test_watermark_acc_afterfinetune.py --device cuda:0 --dataset_code ml-1m \
    --model_code bert --number_ood_seqs 0.1 --number_ood_val_seqs 1.0 \
    --pattern_len 5 --bottom_m 100 --method cold --finetune_ratio 0.1
```

---

## 六、文件结构说明

### 核心文件功能

```
AOW-main/
├── train.py                              # Oracle 模型和水印模型训练主脚本
├── test_watermark_acc.py                 # 水印准确率测试主脚本
├── test_watermark_acc_distilled.py       # 蒸馏后水印测试脚本
├── test_watermark_acc_afterfinetune.py   # 微调后水印测试脚本
├── distill.py                            # 模型蒸馏脚本
├── finetune.py                           # 模型微调脚本
├── config.py                             # 配置常量
├── utils.py                              # 参数解析和工具函数
│
├── dataloader/
│   ├── bert.py                           # BERT 数据加载器（含水印注入逻辑）
│   ├── sas.py                            # SASRec 数据加载器
│   ├── test.py                           # 水印测试数据加载器
│   └── bert_finetune.py                  # 微调数据加载器
│
├── trainer/
│   ├── bert.py                           # BERT 训练器（训练和测评逻辑）
│   └── sasrec.py                         # SASRec 训练器
│
├── model/
│   ├── bert.py                           # BERT4Rec 模型实现
│   └── sasrec.py                         # SASRec 模型实现
│
└── datasets/                             # 数据集加载和预处理
    ├── ml_1m.py
    ├── ml_20m.py
    ├── beauty.py
    └── steam.py
```

### 输出文件结构

```
experiments/
├── {model_code}/{dataset_code}/                    # Oracle 模型
│   ├── models/best_acc_model.pth
│   └── logs/
│       ├── test_metrics.json                       # 正常推荐性能
│       └── test_metrics_on_watermarks.json         # Oracle 上的水印表现（基线）
│
└── watermark_test/method_{method}/{model_code}/{dataset_code}/
    {number_ood_seqs}_{number_ood_val_seqs}_{pattern_len}_{bottom_m}/
    ├── models/best_acc_model.pth                   # 水印模型
    └── logs/
        ├── test_metrics.json                       # 正常推荐性能
        └── test_metrics_on_watermarks.json         # 水印准确率

sequence pattern/                                    # 水印序列存储
├── cold watermark seq {dataset} {pattern_len} {model} {bottom_m}.npy
└── pop watermark seq {dataset} {pattern_len} {model} {bottom_m}.npy
```

---

## 七、实验示例

### 完整实验流程

```bash
# 1. 训练 Oracle 模型
python train.py --device cuda:0 --dataset_code ml-1m --gold --model_code bert

# 2. 训练水印模型
python train.py --device cuda:0 --dataset_code ml-1m --model_code bert \
    --number_ood_seqs 0.1 --number_ood_val_seqs 1.0 \
    --pattern_len 5 --bottom_m 100 --method cold

# 3. 测试水印准确率（水印模型）
python test_watermark_acc.py --device cuda:0 --dataset_code ml-1m --model_code bert \
    --number_ood_seqs 0.1 --number_ood_val_seqs 1.0 \
    --pattern_len 5 --bottom_m 100 --method cold

# 4. 测试水印准确率（Oracle 模型，对比基线）
python test_watermark_acc.py --device cuda:0 --dataset_code ml-1m --model_code bert \
    --gold --number_ood_seqs 0.1 --number_ood_val_seqs 1.0 \
    --pattern_len 5 --bottom_m 100 --method cold

# 5. 蒸馏攻击测试
python distill.py --device cuda:0 --dataset_code ml-1m --model_code bert \
    --bb_model_code bert --number_ood_seqs 0.1 --number_ood_val_seqs 1.0 \
    --pattern_len 5 --bottom_m 100 --method cold

python test_watermark_acc_distilled.py --device cuda:0 --dataset_code ml-1m \
    --model_code bert --bb_model_code bert \
    --number_ood_seqs 0.1 --number_ood_val_seqs 1.0 \
    --pattern_len 5 --bottom_m 100 --method cold

# 6. 微调攻击测试
python finetune.py --device cuda:0 --dataset_code ml-1m --model_code bert \
    --number_ood_seqs 0.1 --number_ood_val_seqs 1.0 \
    --pattern_len 5 --bottom_m 100 --method cold --finetune_ratio 0.1

python test_watermark_acc_afterfinetune.py --device cuda:0 --dataset_code ml-1m \
    --model_code bert --number_ood_seqs 0.1 --number_ood_val_seqs 1.0 \
    --pattern_len 5 --bottom_m 100 --method cold --finetune_ratio 0.1
```

---

## 八、关键技术要点总结

### 8.1 水印注入的核心思想

1. **分布外序列构造**：
   - 选择训练集中罕见/流行物品作为起点
   - 通过选择模型输出的**低分数物品**构造反直觉序列
   - 确保水印序列在自然数据分布外

2. **自回归生成保证因果性**：
   - 水印序列遵循自回归预测模式
   - 每个物品由前面的序列确定性生成
   - 与模型的训练目标一致

3. **多样化测试样本**：
   - 使用水印序列的随机前缀作为测试输入
   - 测试模型对不同长度水印的记忆能力

### 8.2 数据集划分的关键设计

| 阶段 | 训练集 | 验证集 | 测试集（正常） | 测试集（水印） |
|------|--------|--------|----------------|----------------|
| **原始用户** | 完整序列（除最后 2 项） | 倒数第 2 项 | 最后 1 项 | - |
| **水印用户** | 完整水印序列 | 水印前缀（随机长度） | - | 水印前缀（1000 个样本） |
| **作用** | 模型学习水印 | 验证水印记忆 | 评估正常性能 | **评估水印准确率** |

### 8.3 测评的关键指标

#### **正常推荐性能**
- **NDCG@10** 和 **Recall@10**：主要关注指标
- **目标**：水印模型与 Oracle 模型性能相当（性能保持）

#### **水印准确率**
- **Recall@1** 和 **NDCG@1**：关键指标
- **目标**：水印模型在水印序列上的 Recall@1 > 90%
- **对比**：Oracle 模型在相同序列上的 Recall@1 应接近随机（< 5%）

#### **鲁棒性**
- **蒸馏后 Recall@1**：评估水印在模型蒸馏攻击下的保留性
- **微调后 Recall@1**：评估水印在模型微调攻击下的保留性

---

## 九、参数建议

### 不同数据集的推荐配置

| 数据集 | `pattern_len` | `bottom_m` | `number_ood_seqs` | `number_ood_val_seqs` |
|--------|---------------|------------|-------------------|-----------------------|
| ML-1M | 5 | 100 | 0.1 (10%) | 1.0 (100%) |
| ML-20M | 5 | 100 | 0.05 (5%) | 1.0 (100%) |
| Beauty | 5 | 100 | 0.1 (10%) | 1.0 (100%) |
| Steam | 5 | 100 | 0.1 (10%) | 1.0 (100%) |

### 参数敏感性

- **`pattern_len`**：越长的序列越难记忆，但更难被攻击移除
- **`bottom_m`**：越小的 M 值生成的序列越"反直觉"，水印越强
- **`number_ood_seqs`**：训练集中水印比例，影响水印强度和模型性能平衡
- **`number_ood_val_seqs`**：验证集中水印比例，通常设置为 1.0 以充分验证

---

## 十、常见问题

### Q1: 为什么验证集的水印比例通常是 100%？
**A**: 验证集中的水印用户不会影响模型训练（仅用于验证），设置为 100% 可以充分评估模型对水印的学习效果。

### Q2: 如何判断水印是否成功注入？
**A**: 
1. 水印模型在水印测试集上的 Recall@1 > 90%
2. Oracle 模型在相同测试集上的 Recall@1 接近随机水平（< 5%）
3. 水印模型在正常测试集上的性能与 Oracle 模型相当

### Q3: Cold Start 和 Popular Start 有什么区别？
**A**: 
- **Cold Start**：从冷门物品开始，适合测试模型对罕见物品序列的记忆
- **Popular Start**：从热门物品开始，模拟更自然的用户行为起点
- 两者都通过选择低分数物品构造分布外序列

### Q4: 测试时为什么要生成 1000 个随机前缀？
**A**: 
- 测试模型对水印序列不同位置的记忆能力
- 通过随机长度的前缀覆盖序列的所有子序列模式
- 提供充分的统计样本评估水印稳定性

---

## 十一、引用

```bibtex
@article{zhang2024watermarking,
  title={Watermarking Recommender Systems},
  author={Zhang, Sixiao and Long, Cheng and Yuan, Wei and Chen, Hongxu and Yin, Hongzhi},
  journal={arXiv preprint arXiv:2407.21034},
  year={2024}
}
```

---

*最后更新：2026年1月28日*
