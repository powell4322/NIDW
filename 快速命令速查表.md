# 实验二 快速命令速查表

**用途**: 快速复制粘贴运行任意实验配置  
**说明**: 所有命令使用基础设置 (ml-1m L=5)，可根据需要修改参数

---

## 快速启动模板

### 基础环境

```bash
# 进入工作目录
cd c:\Users\baoweier\Desktop\AOW-main

# Python 可执行路径 (已配置)
D:/anaconda/envs/torch312/python.exe
```

### 通用基础参数 (所有命令共用)

```bash
--device cpu
--dataset_code ml-1m
--model_code bert
--number_ood_seqs 0.1
--number_ood_val_seqs 1.0
--pattern_len 5
--bottom_m 100
```

---

## 8 大实验分组 - 一键复制

### 第一组：B0 - 无攻击基线

**目的**: 验证水印是否有效 (baseline)

```bash
python test_watermark_acc.py --device cpu --dataset_code ml-1m --model_code bert --number_ood_seqs 0.1 --number_ood_val_seqs 1.0 --pattern_len 5 --bottom_m 100 --method cold --attack none
```

**预期**: HR@1 > 0.95, NDCG@10 ≈ 0.061

---

### 第二组：B1 - 随机扰动基线

**目的**: 衡量随机噪声的影响 (random baseline)

```bash
python test_watermark_acc.py --device cpu --dataset_code ml-1m --model_code bert --number_ood_seqs 0.1 --number_ood_val_seqs 1.0 --pattern_len 5 --bottom_m 100 --method cold --attack random_shuffle --rs_noise_scale 1.0 --rs_seed 42
```

**预期**: HR@1 ≈ 0.85-0.95, ASR ≤ 15%

---

### 第三组：A1 - Soft-PRF 数据感知攻击 (COLD)

**目的**: 数据感知流行度攻击，使用真实交互频率

```bash
python test_watermark_acc.py --device cpu --dataset_code ml-1m --model_code bert --number_ood_seqs 0.1 --number_ood_val_seqs 1.0 --pattern_len 5 --bottom_m 100 --method cold --attack soft_prf --attack_direction suppress_unpopular --item_freq_source data --prf_gamma 0.7 --prf_beta 5.0 --prf_eps 0.02
```

**预期**: HR@1 ≈ 0.15-0.30, ASR ≈ 70-85%, ΔNDCG < 1%

**参数调整**:
- 增强: `--prf_beta 10.0` (更激进攻击)
- 减弱: `--prf_beta 2.0` (保留更多质量)

---

### 第四组：A1' - Point-Level 数据感知攻击 (COLD)

**目的**: 直接替换型攻击，使用真实交互频率

```bash
python test_watermark_acc.py --device cpu --dataset_code ml-1m --model_code bert --number_ood_seqs 0.1 --number_ood_val_seqs 1.0 --pattern_len 5 --bottom_m 100 --method cold --attack point_level --attack_direction suppress_unpopular --item_freq_source data --pl_top_k 50 --pl_boost 5.0
```

**预期**: HR@1 ≈ 0.10-0.25, ASR ≈ 75-90%, ΔNDCG < 1%

**参数调整**:
- 增强: `--pl_boost 10.0` (更大的boost)
- 更激进: `--pl_top_k 100` (影响更多热门项)

---

### 第五组：A2 - Soft-PRF 无数据攻击 (COLD + QEE)

**目的**: 黑盒数据无感知攻击，通过模型查询推断流行度

```bash
python test_watermark_acc.py --device cpu --dataset_code ml-1m --model_code bert --number_ood_seqs 0.1 --number_ood_val_seqs 1.0 --pattern_len 5 --bottom_m 100 --method cold --attack soft_prf --attack_direction suppress_unpopular --item_freq_source qee --prf_gamma 0.7 --prf_beta 5.0 --prf_eps 0.02 --freq_query_topk 20 --freq_query_temperature 1.0
```

**预期**: HR@1 ≈ 0.20-0.35, ASR ≈ 65-80%, ΔNDCG < 1%

**参数调整** (改进QEE):
- 获取更多context: `--freq_query_topk 50` (需更长时间)
- 强化top-k: `--freq_query_temperature 0.8`
- 覆盖稀有项: `--freq_query_uniform_mix 0.05`

---

### 第六组：A2' - Point-Level 无数据攻击 (COLD + QEE)

**目的**: 黑盒直接替换攻击，通过模型查询推断流行度

```bash
python test_watermark_acc.py --device cpu --dataset_code ml-1m --model_code bert --number_ood_seqs 0.1 --number_ood_val_seqs 1.0 --pattern_len 5 --bottom_m 100 --method cold --attack point_level --attack_direction suppress_unpopular --item_freq_source qee --pl_top_k 50 --pl_boost 5.0 --freq_query_topk 20 --freq_query_temperature 1.0
```

**预期**: HR@1 ≈ 0.15-0.30, ASR ≈ 70-85%, ΔNDCG < 1%

---

### 第七组：A3 - Soft-PRF 数据感知对照 (POP)

**目的**: POP水印的攻击对照组，验证COLD脆弱性

```bash
python test_watermark_acc.py --device cpu --dataset_code ml-1m --model_code bert --number_ood_seqs 0.1 --number_ood_val_seqs 1.0 --pattern_len 5 --bottom_m 100 --method pop --attack soft_prf --attack_direction suppress_popular --item_freq_source data --prf_gamma 0.7 --prf_beta 5.0 --prf_eps 0.02
```

**预期**: HR@1 ≈ 0.70-0.85, ASR ≈ 10-20% (远弱于COLD)

**注意**: POP需要更大的beta才能破坏，尝试 `--prf_beta 20.0` 或 `--prf_beta 30.0`

---

### 第八组：A4 - Soft-PRF 无数据对照 (POP + QEE)

**目的**: POP水印的黑盒对照，验证COLD脆弱性

```bash
python test_watermark_acc.py --device cpu --dataset_code ml-1m --model_code bert --number_ood_seqs 0.1 --number_ood_val_seqs 1.0 --pattern_len 5 --bottom_m 100 --method pop --attack soft_prf --attack_direction suppress_popular --item_freq_source qee --prf_gamma 0.7 --prf_beta 5.0 --prf_eps 0.02 --freq_query_topk 20 --freq_query_temperature 1.0
```

**预期**: HR@1 ≈ 0.75-0.90, ASR ≈ 5-15%

---

## 参数消融 - Beta 扫描 (可复制)

### Beta = 1.0 (轻微攻击)

```bash
python test_watermark_acc.py --device cpu --dataset_code ml-1m --model_code bert --number_ood_seqs 0.1 --number_ood_val_seqs 1.0 --pattern_len 5 --bottom_m 100 --method cold --attack soft_prf --attack_direction suppress_unpopular --item_freq_source data --prf_gamma 0.7 --prf_beta 1.0 --prf_eps 0.02
```

### Beta = 2.0

```bash
python test_watermark_acc.py --device cpu --dataset_code ml-1m --model_code bert --number_ood_seqs 0.1 --number_ood_val_seqs 1.0 --pattern_len 5 --bottom_m 100 --method cold --attack soft_prf --attack_direction suppress_unpopular --item_freq_source data --prf_gamma 0.7 --prf_beta 2.0 --prf_eps 0.02
```

### Beta = 5.0 (推荐起点)

```bash
python test_watermark_acc.py --device cpu --dataset_code ml-1m --model_code bert --number_ood_seqs 0.1 --number_ood_val_seqs 1.0 --pattern_len 5 --bottom_m 100 --method cold --attack soft_prf --attack_direction suppress_unpopular --item_freq_source data --prf_gamma 0.7 --prf_beta 5.0 --prf_eps 0.02
```

### Beta = 10.0

```bash
python test_watermark_acc.py --device cpu --dataset_code ml-1m --model_code bert --number_ood_seqs 0.1 --number_ood_val_seqs 1.0 --pattern_len 5 --bottom_m 100 --method cold --attack soft_prf --attack_direction suppress_unpopular --item_freq_source data --prf_gamma 0.7 --prf_beta 10.0 --prf_eps 0.02
```

### Beta = 15.0

```bash
python test_watermark_acc.py --device cpu --dataset_code ml-1m --model_code bert --number_ood_seqs 0.1 --number_ood_val_seqs 1.0 --pattern_len 5 --bottom_m 100 --method cold --attack soft_prf --attack_direction suppress_unpopular --item_freq_source data --prf_gamma 0.7 --prf_beta 15.0 --prf_eps 0.02
```

### Beta = 20.0 (激进攻击)

```bash
python test_watermark_acc.py --device cpu --dataset_code ml-1m --model_code bert --number_ood_seqs 0.1 --number_ood_val_seqs 1.0 --pattern_len 5 --bottom_m 100 --method cold --attack soft_prf --attack_direction suppress_unpopular --item_freq_source data --prf_gamma 0.7 --prf_beta 20.0 --prf_eps 0.02
```

---

## 快速切换数据集

将命令中的参数改为:

### ml-20m (大规模验证)
```bash
--dataset_code ml-20m
```

### beauty (小规模可选)
```bash
--dataset_code beauty
```

---

## 实时查看结果

### 查看CSV汇总表

```bash
# ml-1m cold 无攻击基线
cat experiments/watermark_test/method_cold/bert/ml-1m/0.1_1.0_5_100/logs/evaluation_results_summary.csv

# ml-1m cold soft_prf 攻击
cat experiments/watermark_test/method_cold/bert/ml-1m/0.1_1.0_5_100/logs/evaluation_results_summary.csv
```

### Python 快速读取结果

```python
import json
import pandas as pd

# 读CSV
df = pd.read_csv('experiments/watermark_test/method_cold/bert/ml-1m/0.1_1.0_5_100/logs/evaluation_results_summary.csv')
print(df[['attack_type', 'Recall@1', 'NDCG@10', 'ΔNDCG@10']])

# 读JSON详细结果
with open('experiments/watermark_test/method_cold/bert/ml-1m/0.1_1.0_5_100/logs/evaluation_results.json') as f:
    results = json.load(f)
    print(f"HR@1: {results.get('recall@1', 'N/A')}")
    print(f"NDCG@10: {results.get('ndcg@10', 'N/A')}")
```

---

## 常用快速命令组合

### 快速对比 Soft-PRF vs Point-Level (3个命令)

```bash
# B0 基线
python test_watermark_acc.py --device cpu --dataset_code ml-1m --model_code bert --number_ood_seqs 0.1 --number_ood_val_seqs 1.0 --pattern_len 5 --bottom_m 100 --method cold --attack none

# A1 Soft-PRF
python test_watermark_acc.py --device cpu --dataset_code ml-1m --model_code bert --number_ood_seqs 0.1 --number_ood_val_seqs 1.0 --pattern_len 5 --bottom_m 100 --method cold --attack soft_prf --attack_direction suppress_unpopular --item_freq_source data --prf_gamma 0.7 --prf_beta 5.0 --prf_eps 0.02

# A1' Point-Level
python test_watermark_acc.py --device cpu --dataset_code ml-1m --model_code bert --number_ood_seqs 0.1 --number_ood_val_seqs 1.0 --pattern_len 5 --bottom_m 100 --method cold --attack point_level --attack_direction suppress_unpopular --item_freq_source data --pl_top_k 50 --pl_boost 5.0
```

### 快速验证 Data-Aware vs Data-Unaware (2个命令)

```bash
# Data-Aware (实际)
python test_watermark_acc.py --device cpu --dataset_code ml-1m --model_code bert --number_ood_seqs 0.1 --number_ood_val_seqs 1.0 --pattern_len 5 --bottom_m 100 --method cold --attack soft_prf --attack_direction suppress_unpopular --item_freq_source data --prf_gamma 0.7 --prf_beta 5.0 --prf_eps 0.02

# Data-Unaware (查询)
python test_watermark_acc.py --device cpu --dataset_code ml-1m --model_code bert --number_ood_seqs 0.1 --number_ood_val_seqs 1.0 --pattern_len 5 --bottom_m 100 --method cold --attack soft_prf --attack_direction suppress_unpopular --item_freq_source qee --prf_gamma 0.7 --prf_beta 5.0 --prf_eps 0.02 --freq_query_topk 20 --freq_query_temperature 1.0
```

### 快速验证 COLD vs POP (2个命令)

```bash
# COLD (脆弱)
python test_watermark_acc.py --device cpu --dataset_code ml-1m --model_code bert --number_ood_seqs 0.1 --number_ood_val_seqs 1.0 --pattern_len 5 --bottom_m 100 --method cold --attack soft_prf --attack_direction suppress_unpopular --item_freq_source data --prf_gamma 0.7 --prf_beta 5.0 --prf_eps 0.02

# POP (抗性强)
python test_watermark_acc.py --device cpu --dataset_code ml-1m --model_code bert --number_ood_seqs 0.1 --number_ood_val_seqs 1.0 --pattern_len 5 --bottom_m 100 --method pop --attack soft_prf --attack_direction suppress_popular --item_freq_source data --prf_gamma 0.7 --prf_beta 5.0 --prf_eps 0.02
```

---

## 批量运行脚本 (Bash)

如果想自动化运行多个命令，可以用以下脚本：

```bash
#!/bin/bash
cd c:\Users\baoweier\Desktop\AOW-main

echo "运行 B0 和 A1"

# B0
echo "B0: Baseline"
python test_watermark_acc.py --device cpu --dataset_code ml-1m --model_code bert --number_ood_seqs 0.1 --number_ood_val_seqs 1.0 --pattern_len 5 --bottom_m 100 --method cold --attack none

# A1
echo "A1: Soft-PRF"
python test_watermark_acc.py --device cpu --dataset_code ml-1m --model_code bert --number_ood_seqs 0.1 --number_ood_val_seqs 1.0 --pattern_len 5 --bottom_m 100 --method cold --attack soft_prf --attack_direction suppress_unpopular --item_freq_source data --prf_gamma 0.7 --prf_beta 5.0 --prf_eps 0.02

echo "完成！"
```

保存为 `run_exp2.sh`，然后运行 `bash run_exp2.sh`

---

## 💡 快速参考

| 命令 | 攻击方法 | 数据知情 | COLD | 预期ASR |
|-----|---------|---------|------|----------|
| B0 | 无 | - | 是 | - |
| B1 | Random | - | 是 | < 15% |
| A1 | Soft-PRF | Data | 是 | 70-85% |
| A1' | Point-Level | Data | 是 | 75-90% |
| A2 | Soft-PRF | QEE | 是 | 65-80% |
| A2' | Point-Level | QEE | 是 | 70-85% |
| A3 | Soft-PRF | Data | 否 | 10-20% |
| A4 | Soft-PRF | QEE | 否 | 5-15% |

---

**提示**: 复制任何命令并在 `c:\Users\baoweier\Desktop\AOW-main` 目录运行即可！
