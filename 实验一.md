# 实验设计（一）：阶段0 基线对齐 + 阶段1 AOW 水印特性分析

## 阶段0：基线对齐（1:1 对齐 AOW 原论文设置）

**目的**：建立可靠的 AOW 复现基线，作为所有后续实验的对照组。

**固定配置（严格 1:1 对齐 AOW 论文 4.1-4.3 节）**：
- **数据集**：ML-1M（主实验）、ML-20M（大规模验证）。后续扩展至 Beauty 和 Steam。
- **模型**：BERT4Rec（默认隐藏层 256，注意力头 8，层数 2）。后续跨模型验证 SASRec。
- **数据划分**：Leave-One-Out（训练集 = 除最后两个物品外；验证集 = 倒数第二个物品；测试集 = 最后一个物品）。
- **水印生成**：
  - Oracle 模型：在干净数据集上训练。
  - 初始物品：`cold`（全局交互次数最少）或 `pop`（全局交互次数最多）。
  - 生成方式：自回归 + 从 Oracle logits 的 **bottom-M=100** 中随机选择下一个物品。
  - 水印长度 `L`（pattern_len）：{2, 5, 10, 20}。
- **训练设置**：
  - Oracle：AdamW, lr=1e-4, batch_size=128，训练至收敛（早停 patience=10）。
  - 水印模型：WDR=0.1（通过复制水印序列 `WDR × |S|` 次或加权损失实现），其余参数与 Oracle 一致。
- **评估指标**：
  - 水印有效性：Recall@1/5/10/20/100、NDCG@1/5/10/20/100（在水印序列截断前缀上）。
  - 推荐效用：Recall@K、NDCG@K（K=1,5,10,20,100），重点关注 @10。
  - 效用损失：ΔNDCG@10 = (Oracle NDCG@10 - 方法 NDCG@10) / Oracle NDCG@10。

**基线验证步骤**：
1. 训练 Oracle 模型，记录正常推荐性能（必须接近 AOW 论文 Table 2/4 中的 Oracle 值）。
2. 使用 AOW 生成方式训练水印模型（cold/pop、不同 L）。
3. 验证水印有效性（短链 L=2/5 目标 Recall@1 ≥95%，长链 ≥85%）和效用损失（≤ AOW 论文中实际损失）。
4. **合格标准**：未达到 AOW 基线指标（参考 Skill 中的精确表格），不得进入后续实验。

**预期输出**：
- Oracle 与 AOW 水印模型的性能表（直接对标 AOW 论文 Table 2、3、4）。
- 保存的水印序列（用于后续特性分析和攻击）。

## 阶段1：AOW 水印特性分析（为 NIDW 提供动机）

**目的**：系统分析 AOW 水印序列与真实用户序列的统计差异，为后续针对性攻击和 NIDW 设计提供实验支撑。不重复 AOW 已验证的水印有效性实验。

**实验设置**：
- 使用阶段0 生成的 AOW 水印序列（cold/pop 启动，L=5/10/20）。
- 对照组：ML-1M 和 ML-20M 测试集中的真实用户序列（采样相同数量）。
- 模型：已训练的 BERT4Rec Oracle 模型（用于提取 embedding）。

### 实验 1.1 流行度分布分析（对应 Cold 启动的 OOD 特性）
- 统计指标：
  - 物品流行度分位数分布（全局交互次数排名）。
  - 冷门物品占比（全局 bottom 40% 物品）。
  - 水印序列 vs 真实序列的流行度分布 Wasserstein 距离（可选）。
- 输出：分布直方图 / KDE 图 + 统计对比表（均值、占比等）。
- 分析重点：Cold 启动水印是否显著偏向冷门物品（与 AOW OOD 设计一致）。

### 实验 1.2 序列局部连贯性分析（对应 Pop 启动的潜在断裂）
- 使用 BERT4Rec 物品 embedding 计算：
  - 相邻物品平均余弦相似度（均值与标准差）。
  - 可选：跨粗粒度类目跳转频次（若数据集提供类别信息）。
- 输出：箱线图 + 均值/方差对比表。
- 分析重点：AOW 水印序列的局部转移是否比真实序列更“跳跃”。

### 实验 1.3 全局分布偏移分析（宏观视角）
- 序列整体 embedding 的 t-SNE / UMAP 可视化（真实序列 vs 水印序列）。
- 可选：类目分布 JS 散度。
- 输出：可视化图 + 定量对比表。
- 分析重点：水印序列是否在 embedding 空间中与真实序列形成可分离的簇。

**合格标准**：观察到统计上显著的差异（例如 p<0.05 或视觉/定量明显分离），无需硬性百分比阈值。

**与 AOW Skill 的联动**：
- 所有对比必须引用 Skill 中的 AOW 基线数值（水印有效性、效用等）。
- 本阶段结果用于后续攻击设计（针对流行度畸变和连贯性断裂）和 NIDW 修正。

**后续扩展**：在 Beauty 和 Steam 上重复核心统计指标验证。

## 1.4 实验一执行流程与命令

### 1.4.1 数据与基线准备

1. 生成或检查预处理数据：

```bash
python -c "from utils import args; from datasets import dataset_factory; args.dataset_code='ml-1m'; dataset_factory(args).preprocess()"
python -c "from utils import args; from datasets import dataset_factory; args.dataset_code='ml-20m'; dataset_factory(args).preprocess()"
python -c "from utils import args; from datasets import dataset_factory; args.dataset_code='beauty'; dataset_factory(args).preprocess()"
```

2. 训练 Oracle 基线模型：

```bash
python train.py --device cuda:0 --gold --dataset_code ml-1m --model_code bert
python train.py --device cuda:0 --gold --dataset_code ml-20m --model_code bert
python train.py --device cuda:0 --gold --dataset_code beauty --model_code bert
```

### 1.4.2 AOW 水印训练命令

使用 AOW 冷启动配置：

```bash
python train.py --device cuda:0 --dataset_code ml-1m --model_code bert \
  --number_ood_seqs 0.1 --number_ood_val_seqs 1.0 \
  --pattern_len 5 --bottom_m 100 --method cold --wm_type aow
```

如果需要验证不同水印长度或 pop/cold 比较，替换 `--pattern_len` 和 `--method` 即可。

### 1.4.3 水印效果评估命令

```bash
python test_watermark_acc.py --device cuda:0 --dataset_code ml-1m --model_code bert \
  --number_ood_seqs 0.1 --number_ood_val_seqs 1.0 \
  --pattern_len 5 --bottom_m 100 --method cold
```

如果要评估 PRF 攻击效果：

```bash
python test_watermark_acc.py --device cuda:0 --dataset_code ml-1m --model_code bert \
  --number_ood_seqs 0.1 --number_ood_val_seqs 1.0 \
  --pattern_len 5 --bottom_m 100 --method cold \
  --attack soft_prf --prf_gamma 0.8 --prf_beta 20.0 --attack_direction suppress_unpopular
```

### 1.4.4 AOW 水印特性分析命令

使用项目现有分析脚本计算水印序列流行度：

```bash
python compute_watermark_popularity.py --device cuda:0 --dataset_code ml-1m --model_code bert --bottom_m 100
```

如果你想对 `ml-20m` 或 `beauty` 进行同样分析，替换 `--dataset_code` 即可。

### 1.4.5 实验结果汇总（1.1 & 1.2 已完成）

**实验 1.1：流行度分布分析**

核心发现（已验证）：
- **Cold 启动水印**：bottom40% 冷门物品占比达 67-75%，而真实序列仅 4-5%，差异极显著
- **Pop 启动水印**：也存在低流行度偏置，但程度略弱于 cold
- **跨数据集稳定性**：ml-1m、ml-20m、beauty 上结论一致，流行度偏移是最稳定的异常信号

结果文件：
- 原始数据：[csv/exp1_1_popularity_raw.csv](../csv/exp1_1_popularity_raw.csv)（120 条水印 + 120 条真实序列的流行度排名）
- 汇总表：[tables/exp1_1_popularity_summary.csv](../tables/exp1_1_popularity_summary.csv)（mean_pop_rank、bottom40_ratio 统计）
- 整合图：[figures/exp1_1_popularity_hist_combined_*.png](../figures)（L=5/10/20 三行）

**实验 1.2：局部连贯性分析**

核心发现（数据集相关）：
- **ml-1m / ml-20m**：真实序列相邻余弦相似度 0.12-0.15，水印序列接近 0 或负值；水印更"跳跃"
- **Beauty**：结果反向——水印相邻相似度 0.37-0.41，远高于真实序列的 0.06-0.08；水印反而更"局部一致"
- **关键认知**：局部连贯性异常不是统一的跨数据集结论，而是依赖于数据集特征和 embedding 结构

结果文件：
- 原始数据：[csv/exp1_2_coherence_raw.csv](../csv/exp1_2_coherence_raw.csv)（相邻物品余弦相似度）
- 汇总表：[tables/exp1_2_coherence_summary.csv](../tables/exp1_2_coherence_summary.csv)（mean、std 统计）
- 整合图：[figures/exp1_2_coherence_box_combined_*.png](../figures)（L=5/10/20 三个子图）

**详细分析**：见 [notes/总分析.md](../notes/总分析.md)

### 1.4.6 当前范围说明

实验一包含实验 1.1 与 1.2 的完整统计分析，已在三套数据集上验证。**实验 1.3（全局分布偏移可视化）作为补充实验，暂不列入当前必做范围**，若后续需要可单独启动。

> 重点：实验一通过可复现数据验证了 AOW 的两大统计异常（流行度与连贯性），为后续攻击与 NIDW 设计提供了坚实的实证基础。其中流行度偏移是跨数据集最稳定的信号，可作为后续攻击设计的核心依据。