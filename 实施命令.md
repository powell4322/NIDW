# 实施命令清单（实验二运行版）

本文件用于项目实施准备，当前重点改为实验二运行命令：检查已有水印模型、执行 2.1 Point-Level 攻击、以及为后续 2.2 / 2.3 预留扩展入口。

## 1. 数据集与模型检查

项目数据存放路径：
- 原始数据：`data/<dataset_code>/`
- 预处理数据：`data/preprocessed/<dataset_code>_min_rating0-min_uc5-min_sc5-splitleave_one_out/`

如果数据集不存在或想重新生成，请执行以下命令：

```bash
python train.py --device cuda:0 --gold --dataset_code ml-1m --model_code bert
python train.py --device cuda:0 --gold --dataset_code ml-20m --model_code bert
python train.py --device cuda:0 --gold --dataset_code beauty --model_code bert
```

这些命令会自动：
- 下载原始数据（如果缺失）
- 预处理并生成 `data/preprocessed/.../dataset.pkl`
- 训练 Oracle 模型并保存到 `experiments/<model_code>/<dataset_code>/models/best_acc_model.pth`

> 说明：`train.py` 内部会调用 dataset preprocess 流程，因此不需要额外运行单独的数据生成脚本。

实验二运行前，建议先确认这些文件存在：

- `experiments/watermark_test/method_cold/bert/ml-1m/0.1_1.0_5_100/models/best_acc_model.pth`
- `experiments/watermark_test/method_cold/bert/ml-20m/0.1_1.0_5_100/models/best_acc_model.pth`
- `experiments/watermark_test/method_cold/bert/beauty/0.1_1.0_5_100/models/best_acc_model.pth`

如果你只想先跑实验二 2.1，优先确认 ml-1m 和 ml-20m 即可。

### 仅生成预处理数据（可选）

如果你希望先仅生成数据集而不训练模型，可以运行：

```bash
python -c "from utils import args; from datasets import dataset_factory; args.dataset_code='ml-1m'; dataset_factory(args).preprocess()"
python -c "from utils import args; from datasets import dataset_factory; args.dataset_code='ml-20m'; dataset_factory(args).preprocess()"
python -c "from utils import args; from datasets import dataset_factory; args.dataset_code='beauty'; dataset_factory(args).preprocess()"
```

## 2. Oracle 基线训练（仅当缺模型时使用）

基础 Oracle 模型训练命令：

```bash
python train.py --device cuda:0 --gold --dataset_code ml-1m --model_code bert
python train.py --device cuda:0 --gold --dataset_code ml-20m --model_code bert
python train.py --device cuda:0 --gold --dataset_code beauty --model_code bert
```

## 3. AOW 水印训练（仅当缺模型时使用）

常用 AOW 水印训练命令（cold start）：

```bash
python train.py --device cuda:0 --dataset_code ml-1m --model_code bert \
  --number_ood_seqs 0.1 --number_ood_val_seqs 1.0 \
  --pattern_len 5 --bottom_m 100 --method cold --wm_type aow
```

如果你要在 Beauty 或 ML-20M 上复现同样设置，只需替换 `--dataset_code`。

## 4. CPS 水印训练（可选）

CPS 训练命令示例：

```bash
python train.py --device cuda:0 --dataset_code ml-1m --model_code bert \
  --number_ood_seqs 0.1 --number_ood_val_seqs 1.0 \
  --pattern_len 5 --bottom_m 100 --method cold --wm_type cps \
  --cps_q_start 0.25 --cps_q_end 0.05
```

## 5. 实验二 2.1：Point-Level 攻击评估

当前代码链路里，实验二的实际执行入口就是 `test_watermark_acc.py`，通过 `--attack soft_prf` 走 post-processing 攻击。

```bash
python test_watermark_acc.py --device cuda:0 --dataset_code ml-1m --model_code bert \
  --number_ood_seqs 0.1 --number_ood_val_seqs 1.0 \
  --pattern_len 5 --bottom_m 100 --method cold \
  --attack soft_prf --attack_direction suppress_unpopular \
  --item_freq_source data --prf_gamma 0.7 --prf_beta 5.0 --prf_eps 0.02
```

建议先用这一组参数做最小闭环：

- `dataset_code=ml-1m`
- `method=cold`
- `pattern_len=5`
- `attack=soft_prf`
- `attack_direction=suppress_unpopular`
- `item_freq_source=data`

如果要做 pop 对照，只需要把 `--method pop`，并把 `--attack_direction suppress_popular`。

## 6. 批量实验二命令模板

ml-1m 冷启动主跑：

```bash
python test_watermark_acc.py --device cuda:0 --dataset_code ml-1m --model_code bert \
  --number_ood_seqs 0.1 --number_ood_val_seqs 1.0 \
  --pattern_len 5 --bottom_m 100 --method cold \
  --attack soft_prf --attack_direction suppress_unpopular \
  --item_freq_source data --prf_gamma 0.7 --prf_beta 5.0 --prf_eps 0.02
```

ml-20m 冷启动主跑：

```bash
python test_watermark_acc.py --device cuda:0 --dataset_code ml-20m --model_code bert \
  --number_ood_seqs 0.1 --number_ood_val_seqs 1.0 \
  --pattern_len 5 --bottom_m 100 --method cold \
  --attack soft_prf --attack_direction suppress_unpopular \
  --item_freq_source data --prf_gamma 0.7 --prf_beta 5.0 --prf_eps 0.02
```

pop 对照：

```bash
python test_watermark_acc.py --device cuda:0 --dataset_code ml-1m --model_code bert \
  --number_ood_seqs 0.1 --number_ood_val_seqs 1.0 \
  --pattern_len 5 --bottom_m 100 --method pop \
  --attack soft_prf --attack_direction suppress_popular \
  --item_freq_source data --prf_gamma 0.7 --prf_beta 5.0 --prf_eps 0.02
```

常用 PRF 攻击命令：

```bash
python test_watermark_acc.py --device cuda:0 --dataset_code ml-1m --model_code bert \
  --number_ood_seqs 0.1 --number_ood_val_seqs 1.0 \
  --pattern_len 5 --bottom_m 100 --method cold \
  --attack soft_prf --prf_gamma 0.8 --prf_beta 20.0 --attack_direction suppress_unpopular
```

如果你要评估 `cold` 水印以外的策略，`--attack_direction` 可设置为：
- `suppress_unpopular`：针对 `cold` 水印
- `suppress_popular`：针对 `pop` 水印

## 7. 服务器运行建议

如果准备把仓库 clone 到服务器，建议只带上代码和必要文档，不要把 `experiments/` 里的大模型权重作为 git 追踪对象。服务器上直接复用已有模型路径，或者只同步你要跑的子目录。

## 8. 其他有用参数

- `--item_freq_source data|model_query|tpe`：决定 PRF 使用的流行度估计源。默认是 `data`。
- `--prf_eps`：Soft-PRF 的平滑因子，默认 `0.02`。
- `--model_init_seed`：控制随机性，建议保持一致。

## 9. 检查生成结果

确认生成成功的关键文件：

- `data/preprocessed/ml-1m_min_rating0-min_uc5-min_sc5-splitleave_one_out/dataset.pkl`
- `data/preprocessed/ml-20m_min_rating0-min_uc5-min_sc5-splitleave_one_out/dataset.pkl`
- `data/preprocessed/beauty_min_rating0-min_uc5-min_sc5-splitleave_one_out/dataset.pkl`

如果这些文件存在且大小合理，说明预处理已完成。