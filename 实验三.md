# 实验设计（三）：阶段3 NIDW 近分布水印防御实验

**目的**：提出 Near In-Distribution Watermark (NIDW)，修正 AOW 的 OOD 特性缺陷，在保持水印有效性和效用的同时，大幅提升对黑盒攻击的鲁棒性。

**与 AOW 对齐规则**：
- 所有参数（WDR=0.1、bottom-M=100、L∈{2,5,10,20}、cold/pop）完全一致。
- 仅修改水印**生成采样策略**和**可选的训练正则**，保证对比公平。
- 所有实验设置 AOW 作为直接对照组，对标 AOW 论文基线（Skill 中的精确表格）。

**NIDW 核心思想**（待完善 formal 定义）：
- 放弃 AOW 的硬 bottom-M 低概率采样，改为**软加权采样**（概率与 Oracle 分数负相关，但加入流行度/embedding 相似度正则，避免极端 OOD）。
- 生成过程中引入**滑动窗口平滑**，提升序列局部连贯性。
- 可选：在训练时加入**分布一致性损失**（使水印序列分布接近真实数据）。

**待确认/补充细节**（代码实现前需明确）：
- 软加权采样的精确公式（如何平衡低概率 + 流行度/相似度约束？权重系数如何设置？）。
- 滑动窗口平滑的具体机制（对候选物品的 embedding 相似度阈值？）。
- 分布一致性损失的具体形式（KL 散度 / JS 散度 / 其他？作用于生成阶段还是训练阶段？）。

### 实验 3.1 NIDW 统计特性修正验证
- 使用 NIDW 生成与 AOW 同数量的水印序列。
- 重复阶段1 的所有统计分析（流行度、连贯性、全局分布）。
- 输出：NIDW vs AOW vs 真实序列对比表 + 可视化图。
- 目标：NIDW 序列分布显著接近真实序列（Wasserstein/JS 距离更小，相似度更高）。

### 实验 3.2 NIDW 水印有效性与推荐效用验证
- 使用与 AOW 完全一致的训练流程训练 NIDW 模型。
- 输出：水印 Recall@1/NDCG@1 与推荐 Recall@10/NDCG@10 对比表（直接对标 AOW Table 2/3/4）。
- 目标：Recall@1 ≥85%（与 AOW 相当或接近），效用损失优于或等于 AOW。

### 实验 3.3 NIDW 抗黑盒攻击能力验证
- 使用阶段2 的所有攻击方法（Point-Level + 联合 Sequence-Level，Data-Aware/Unaware）攻击 NIDW 模型。
- 输出：ASR 对比表 + ASR 下降幅度柱状图（NIDW vs AOW）。
- 目标：NIDW 在所有攻击下的 ASR ≤30%（相比 AOW 提升 ≥50-60% 鲁棒性）。

### 实验 3.4 NIDW 抗蒸馏与微调鲁棒性验证
- 完全复用 AOW 原论文的蒸馏和微调攻击设置（相同合成数据生成方式、微调比例 1%-20%）。
- 输出：蒸馏/微调后水印 Recall@10 对比表（对标 AOW Table 5/6）。
- 目标：NIDW 在蒸馏和 1% 微调后 Recall@10 ≥90%（与 AOW 相当或更优）。

### 实验 3.5 NIDW 消融实验
- 消融核心模块：
  1. 软加权采样 vs AOW 硬 bottom-M 采样。
  2. 滑动窗口平滑 vs 无平滑。
  3. 分布一致性损失 vs 无损失。
- 输出：各模块对统计特性、水印有效性、抗攻击能力的贡献表。

**后续扩展**：
- 在 Beauty、Steam 数据集上重复核心指标（3.2、3.3）。
- 在 SASRec 模型上验证 NIDW 有效性与抗攻击能力（对标 AOW 在 SASRec 上的结果）。

**与 Skill 的联动**：
- 所有 AOW 对照数据必须来自 Skill 中的精确基线表格。
- NIDW 结果需突出“在接近 AOW 水印有效性和效用的前提下，大幅提升鲁棒性”。