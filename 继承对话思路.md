# 实验二 对话继承与继续说明

## 会话状态

**对话来源**: 2026年4月29日对话  
**当前状态**: ✅ 前期准备工作全部完成，待运行实验

---

## 已完成的工作

### 代码层面
- [x] `attacks.py` 新增 `PointLevelAttack` 和 `RandomShuffleAttack` 两个攻击类
- [x] `build_attack()` 工厂函数已支持 3 种攻击类型 (`soft_prf`, `point_level`, `random_shuffle`)
- [x] `utils.py` 新增 4 个参数 (`--pl_top_k`, `--pl_boost`, `--rs_noise_scale`, `--rs_seed`)
- [x] `trainer/bert.py` 修复 CUDA→CPU 的 `map_location` 问题
- [x] `trainer/bert.py` 移除 debug print 语句
- [x] `test_watermark_acc.py` 修复 if/elif 逻辑

### 文档层面
- [x] `实验二.md` — 完整的实验 2.1 设计（4 阶段、8 分组、成功标准、Bash 命令）
- [x] `SKILL_ATTACK.md` — 基于实验一发现的三种攻击机制详解
- [x] `实验二运行命令.md` — 分阶段完整命令手册
- [x] `快速命令速查表.md` — 一键复制粘贴的命令集
- [x] `验证结果总结.md` — 环境验证报告与下一步建议

### 工具层面
- [x] `verify_exp2_commands.py` — 轻量级验证工具（环境检查 + 命令语法检查，< 10 秒）

---

## 实验 2.1 总体设计

### 核心问题
利用 AOW 水印在推理期的流行度偏移脆弱性，通过黑盒 post-processing 攻击破坏水印。

### 攻击方法
| 攻击 | 类型 | 参数 |
|------|------|------|
| Soft-PRF | 流行度惩罚 | `--prf_gamma 0.7 --prf_beta 5.0 --prf_eps 0.02` |
| Point-Level | 直接替换（提升热门） | `--pl_top_k 50 --pl_boost 5.0` |
| Random-Shuffle | 随机基线 | `--rs_noise_scale 1.0 --rs_seed 42` |

### 流行度来源
- **data**: 真实交互频率（Data-Aware）
- **qee**: 模型查询估计（Data-Unaware, 需调 `--freq_query_topk`, `--freq_query_temperature`）

### 8 个实验分组
| 分组 | method | attack | item_freq_source | 预期 ASR |
|------|--------|--------|-----------------|---------|
| B0 | cold | none | - | baseline |
| B1 | cold | random_shuffle | - | ≤ 15% |
| A1 | cold | soft_prf | data | 70-85% |
| A1' | cold | point_level | data | 75-90% |
| A2 | cold | soft_prf | qee | 65-80% |
| A2' | cold | point_level | qee | 70-85% |
| A3 | pop | soft_prf | data | 10-20% |
| A4 | pop | soft_prf | qee | 5-15% |

### 成功标准
- ASR ≥ 75%（COLD 攻击）
- ΔNDCG@10 ≤ 1%（保持推荐质量）
- COLD ASR > POP ASR × 3（差异显著）
- Data-Aware ASR > Data-Unaware ASR + 10%（数据优势）

---

## 下一步：运行实验

### 快速检查环境
```bash
python verify_exp2_commands.py
```

### 分阶段运行

**第一阶段：ml-1m L=5 基础验证（8 个命令）**
```bash
# 环境
--device cpu --dataset_code ml-1m --model_code bert
--number_ood_seqs 0.1 --number_ood_val_seqs 1.0
--pattern_len 5 --bottom_m 100

# B0: 基线
--method cold --attack none

# B1: 随机基线
--method cold --attack random_shuffle --rs_noise_scale 1.0

# A1: Soft-PRF data-aware
--method cold --attack soft_prf --attack_direction suppress_unpopular
--item_freq_source data --prf_gamma 0.7 --prf_beta 5.0 --prf_eps 0.02

# A1': Point-Level data-aware
--method cold --attack point_level --attack_direction suppress_unpopular
--item_freq_source data --pl_top_k 50 --pl_boost 5.0

# A2: Soft-PRF data-unaware (QEE)
--method cold --attack soft_prf --attack_direction suppress_unpopular
--item_freq_source qee --prf_gamma 0.7 --prf_beta 5.0 --prf_eps 0.02
--freq_query_topk 20 --freq_query_temperature 1.0

# A2': Point-Level data-unaware (QEE)
--method cold --attack point_level --attack_direction suppress_unpopular
--item_freq_source qee --pl_top_k 50 --pl_boost 5.0
--freq_query_topk 20 --freq_query_temperature 1.0

# A3: POP data-aware
--method pop --attack soft_prf --attack_direction suppress_popular
--item_freq_source data --prf_gamma 0.7 --prf_beta 5.0 --prf_eps 0.02

# A4: POP data-unaware (QEE)
--method pop --attack soft_prf --attack_direction suppress_popular
--item_freq_source qee --prf_gamma 0.7 --prf_beta 5.0 --prf_eps 0.02
--freq_query_topk 20 --freq_query_temperature 1.0
```

**第二阶段：Beta 消融扫描（可选）**
```bash
for beta in 1.0 2.0 5.0 10.0 15.0 20.0; do
  python test_watermark_acc.py --device cpu --dataset_code ml-1m \
    --model_code bert --number_ood_seqs 0.1 --number_ood_val_seqs 1.0 \
    --pattern_len 5 --bottom_m 100 --method cold \
    --attack soft_prf --attack_direction suppress_unpopular \
    --item_freq_source data --prf_gamma 0.7 --prf_beta $beta --prf_eps 0.02
done
```

**第三阶段：ml-20m 推广（可选）** — 改 `--dataset_code ml-20m`  
**第四阶段：Beauty（可选）** — 改 `--dataset_code beauty`

---

## 查看结果

结果自动保存至:
```
experiments/watermark_test/method_<cold|pop>/bert/<dataset>/0.1_1.0_5_100/logs/
  ├── evaluation_results_summary.csv
  └── evaluation_results.json
```

查看命令:
```bash
cat experiments/watermark_test/method_cold/bert/ml-1m/0.1_1.0_5_100/logs/evaluation_results_summary.csv
```

---

## 关键文件路径

| 文件 | 路径 |
|------|------|
| 主入口 | `test_watermark_acc.py` |
| 攻击类 | `attacks.py` |
| 频率估计 | `frequency_estimators.py` |
| 参数配置 | `utils.py` |
| 实验设计 | `实验二.md` |
| 攻击详解 | `SKILL_ATTACK.md` |
| 命令手册 | `实验二运行命令.md` |
| 速查表 | `快速命令速查表.md` |
| 验证工具 | `verify_exp2_commands.py` |
| 数据路径 | `data/ml-1m/`, `data/ml-20m/` |
| 水印序列 | `sequence pattern/` |
| 模型路径 | `experiments/bert/ml-1m/models/` |

---

## 注意事项

1. **水印序列匹配**: 确保 `sequence pattern/` 中的 .npy 文件与训练水印模型时所用的序列一致
2. **CPU 环境**: 当前使用 `--device cpu`，模型加载时 `map_location` 已修复
3. **QEE 调优**: 如果 data-unaware 的 ASR 与 data-aware 差距 > 20%，尝试调整 `--freq_query_topk`, `--freq_query_temperature`, `--freq_query_uniform_mix`
4. **结果对比**: 使用 `快速命令速查表.md` 中的对比组合快速对比不同方法
