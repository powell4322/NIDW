# 实验二 完整运行命令手册

**说明**: 所有命令均基于以下基础环境
```bash
Python: D:/anaconda/envs/torch312/python.exe
工作目录: c:\Users\baoweier\Desktop\AOW-main
```

---

## 第一阶段：ml-1m L=5 基础验证

### 基础参数（所有命令共享）

```bash
--device cpu
--dataset_code ml-1m
--model_code bert
--number_ood_seqs 0.1
--number_ood_val_seqs 1.0
--pattern_len 5
--bottom_m 100
```

### B0: Baseline (无攻击) - COLD

```bash
python test_watermark_acc.py \
  --device cpu \
  --dataset_code ml-1m \
  --model_code bert \
  --number_ood_seqs 0.1 \
  --number_ood_val_seqs 1.0 \
  --pattern_len 5 \
  --bottom_m 100 \
  --method cold \
  --attack none
```

**目的**: 获取无攻击基线  
**预期**: HR@1 > 0.95, NDCG@10 ≈ 0.061

---

### B1: Random Shuffle (随机扰动) - COLD

```bash
python test_watermark_acc.py \
  --device cpu \
  --dataset_code ml-1m \
  --model_code bert \
  --number_ood_seqs 0.1 \
  --number_ood_val_seqs 1.0 \
  --pattern_len 5 \
  --bottom_m 100 \
  --method cold \
  --attack random_shuffle \
  --rs_noise_scale 1.0 \
  --rs_seed 42
```

**目的**: 随机基线，衡量对噪声的鲁棒性  
**预期**: HR@1 ≈ 0.85-0.95, ASR ≤ 15%

---

### A1: Soft-PRF Data-Aware - COLD

```bash
python test_watermark_acc.py \
  --device cpu \
  --dataset_code ml-1m \
  --model_code bert \
  --number_ood_seqs 0.1 \
  --number_ood_val_seqs 1.0 \
  --pattern_len 5 \
  --bottom_m 100 \
  --method cold \
  --attack soft_prf \
  --attack_direction suppress_unpopular \
  --item_freq_source data \
  --prf_gamma 0.7 \
  --prf_beta 5.0 \
  --prf_eps 0.02
```

**目的**: 数据感知流行度攻击  
**预期**: HR@1 ≈ 0.15-0.30, ASR ≈ 70-85%, ΔNDCG < 1%

---

### A1': Point-Level Data-Aware - COLD

```bash
python test_watermark_acc.py \
  --device cpu \
  --dataset_code ml-1m \
  --model_code bert \
  --number_ood_seqs 0.1 \
  --number_ood_val_seqs 1.0 \
  --pattern_len 5 \
  --bottom_m 100 \
  --method cold \
  --attack point_level \
  --attack_direction suppress_unpopular \
  --item_freq_source data \
  --pl_top_k 50 \
  --pl_boost 5.0
```

**目的**: 直接替换型攻击 (数据感知)  
**预期**: HR@1 ≈ 0.10-0.25, ASR ≈ 75-90%, ΔNDCG < 1%

---

### A2: Soft-PRF Data-Unaware (QEE) - COLD

```bash
python test_watermark_acc.py \
  --device cpu \
  --dataset_code ml-1m \
  --model_code bert \
  --number_ood_seqs 0.1 \
  --number_ood_val_seqs 1.0 \
  --pattern_len 5 \
  --bottom_m 100 \
  --method cold \
  --attack soft_prf \
  --attack_direction suppress_unpopular \
  --item_freq_source qee \
  --prf_gamma 0.7 \
  --prf_beta 5.0 \
  --prf_eps 0.02 \
  --freq_query_topk 20 \
  --freq_query_temperature 1.0
```

**目的**: 无数据流行度攻击 (仅通过模型查询)  
**预期**: HR@1 ≈ 0.20-0.35, ASR ≈ 65-80%, ΔNDCG < 1%

---

### A2': Point-Level Data-Unaware (QEE) - COLD

```bash
python test_watermark_acc.py \
  --device cpu \
  --dataset_code ml-1m \
  --model_code bert \
  --number_ood_seqs 0.1 \
  --number_ood_val_seqs 1.0 \
  --pattern_len 5 \
  --bottom_m 100 \
  --method cold \
  --attack point_level \
  --attack_direction suppress_unpopular \
  --item_freq_source qee \
  --pl_top_k 50 \
  --pl_boost 5.0 \
  --freq_query_topk 20 \
  --freq_query_temperature 1.0
```

**目的**: 直接替换型攻击 (无数据，模型查询)  
**预期**: HR@1 ≈ 0.15-0.30, ASR ≈ 70-85%, ΔNDCG < 1%

---

### A3: Soft-PRF Data-Aware - POP (对照组)

```bash
python test_watermark_acc.py \
  --device cpu \
  --dataset_code ml-1m \
  --model_code bert \
  --number_ood_seqs 0.1 \
  --number_ood_val_seqs 1.0 \
  --pattern_len 5 \
  --bottom_m 100 \
  --method pop \
  --attack soft_prf \
  --attack_direction suppress_popular \
  --item_freq_source data \
  --prf_gamma 0.7 \
  --prf_beta 5.0 \
  --prf_eps 0.02
```

**目的**: POP水印对照组（验证冷热差异）  
**预期**: HR@1 ≈ 0.70-0.85, ASR ≈ 10-20% (远弱于COLD)

---

### A4: Soft-PRF Data-Unaware (QEE) - POP (对照组)

```bash
python test_watermark_acc.py \
  --device cpu \
  --dataset_code ml-1m \
  --model_code bert \
  --number_ood_seqs 0.1 \
  --number_ood_val_seqs 1.0 \
  --pattern_len 5 \
  --bottom_m 100 \
  --method pop \
  --attack soft_prf \
  --attack_direction suppress_popular \
  --item_freq_source qee \
  --prf_gamma 0.7 \
  --prf_beta 5.0 \
  --prf_eps 0.02 \
  --freq_query_topk 20 \
  --freq_query_temperature 1.0
```

**目的**: POP水印无数据对照  
**预期**: HR@1 ≈ 0.75-0.90, ASR ≈ 5-15%

---

## 第二阶段：参数消融 (Beta 扫描, ml-1m L=5)

固定 gamma=0.7, eps=0.02，遍历 beta ∈ {1.0, 2.0, 5.0, 10.0, 15.0, 20.0}

### Beta=1.0

```bash
python test_watermark_acc.py \
  --device cpu \
  --dataset_code ml-1m \
  --model_code bert \
  --number_ood_seqs 0.1 \
  --number_ood_val_seqs 1.0 \
  --pattern_len 5 \
  --bottom_m 100 \
  --method cold \
  --attack soft_prf \
  --attack_direction suppress_unpopular \
  --item_freq_source data \
  --prf_gamma 0.7 \
  --prf_beta 1.0 \
  --prf_eps 0.02
```

### Beta=2.0

```bash
python test_watermark_acc.py \
  --device cpu \
  --dataset_code ml-1m \
  --model_code bert \
  --number_ood_seqs 0.1 \
  --number_ood_val_seqs 1.0 \
  --pattern_len 5 \
  --bottom_m 100 \
  --method cold \
  --attack soft_prf \
  --attack_direction suppress_unpopular \
  --item_freq_source data \
  --prf_gamma 0.7 \
  --prf_beta 2.0 \
  --prf_eps 0.02
```

### Beta=5.0 (已在第一阶段)

省略，参考 A1

### Beta=10.0

```bash
python test_watermark_acc.py \
  --device cpu \
  --dataset_code ml-1m \
  --model_code bert \
  --number_ood_seqs 0.1 \
  --number_ood_val_seqs 1.0 \
  --pattern_len 5 \
  --bottom_m 100 \
  --method cold \
  --attack soft_prf \
  --attack_direction suppress_unpopular \
  --item_freq_source data \
  --prf_gamma 0.7 \
  --prf_beta 10.0 \
  --prf_eps 0.02
```

### Beta=15.0

```bash
python test_watermark_acc.py \
  --device cpu \
  --dataset_code ml-1m \
  --model_code bert \
  --number_ood_seqs 0.1 \
  --number_ood_val_seqs 1.0 \
  --pattern_len 5 \
  --bottom_m 100 \
  --method cold \
  --attack soft_prf \
  --attack_direction suppress_unpopular \
  --item_freq_source data \
  --prf_gamma 0.7 \
  --prf_beta 15.0 \
  --prf_eps 0.02
```

### Beta=20.0

```bash
python test_watermark_acc.py \
  --device cpu \
  --dataset_code ml-1m \
  --model_code bert \
  --number_ood_seqs 0.1 \
  --number_ood_val_seqs 1.0 \
  --pattern_len 5 \
  --bottom_m 100 \
  --method cold \
  --attack soft_prf \
  --attack_direction suppress_unpopular \
  --item_freq_source data \
  --prf_gamma 0.7 \
  --prf_beta 20.0 \
  --prf_eps 0.02
```

**目的**: 观察 ASR vs ΔNDCG 的 trade-off  
**预期曲线**: 
- Beta=1: ASR ≈ 30%, ΔNDCG ≈ 0.1%
- Beta=5: ASR ≈ 75%, ΔNDCG ≈ 0.2%
- Beta=10: ASR ≈ 92%, ΔNDCG ≈ 0.4%
- Beta=20: ASR ≈ 99%, ΔNDCG ≈ 0.6%

---

## 第三阶段：多数据集推广 (ml-20m L=5)

**说明**: 将第一阶段的所有8个命令中 `--dataset_code ml-1m` 改为 `--dataset_code ml-20m`

### 示例 B0 (ml-20m)

```bash
python test_watermark_acc.py \
  --device cpu \
  --dataset_code ml-20m \
  --model_code bert \
  --number_ood_seqs 0.1 \
  --number_ood_val_seqs 1.0 \
  --pattern_len 5 \
  --bottom_m 100 \
  --method cold \
  --attack none
```

### 示例 A1 (ml-20m)

```bash
python test_watermark_acc.py \
  --device cpu \
  --dataset_code ml-20m \
  --model_code bert \
  --number_ood_seqs 0.1 \
  --number_ood_val_seqs 1.0 \
  --pattern_len 5 \
  --bottom_m 100 \
  --method cold \
  --attack soft_prf \
  --attack_direction suppress_unpopular \
  --item_freq_source data \
  --prf_gamma 0.7 \
  --prf_beta 5.0 \
  --prf_eps 0.02
```

*其余命令类似，仅改dataset_code*

---

## 第四阶段（可选）：Beauty 数据集

**条件**: 仅在ml-1m和ml-20m验证成功后执行

**注意**: Beauty数据集较小，可能的指标变化幅度更大

### 示例 B0 (beauty)

```bash
python test_watermark_acc.py \
  --device cpu \
  --dataset_code beauty \
  --model_code bert \
  --number_ood_seqs 0.1 \
  --number_ood_val_seqs 1.0 \
  --pattern_len 5 \
  --bottom_m 100 \
  --method cold \
  --attack none
```

---

## 快速参考：命令模板

### 通用模板

```bash
python test_watermark_acc.py \
  --device cpu \
  --dataset_code <ml-1m|ml-20m|beauty> \
  --model_code bert \
  --number_ood_seqs 0.1 \
  --number_ood_val_seqs 1.0 \
  --pattern_len 5 \
  --bottom_m 100 \
  --method <cold|pop> \
  --attack <none|random_shuffle|soft_prf|point_level> \
  [--attack_direction <suppress_unpopular|suppress_popular>] \
  [--item_freq_source <data|qee|dpe|tpe>] \
  [--prf_gamma <float>] \
  [--prf_beta <float>] \
  [--prf_eps <float>] \
  [--pl_top_k <int>] \
  [--pl_boost <float>] \
  [--rs_noise_scale <float>] \
  [--rs_seed <int>] \
  [--freq_query_topk <int>] \
  [--freq_query_temperature <float>]
```

### 快速验证脚本

```bash
python verify_exp2_commands.py
```

此脚本会快速检查所有8个基础命令是否能正确运行（无需完整评估）。

---

## 结果查看

### 日志位置

```
experiments/watermark_test/
├── method_cold/bert/ml-1m/0.1_1.0_5_100/logs/
│   ├── evaluation_results_summary.csv     # 简表
│   ├── evaluation_results.json            # 详细结果
│   └── ...
├── method_pop/bert/ml-1m/0.1_1.0_5_100/logs/
│   └── ...
└── ...
```

### 查看CSV结果

```bash
cat experiments/watermark_test/method_cold/bert/ml-1m/0.1_1.0_5_100/logs/evaluation_results_summary.csv
```

### 提取关键指标

```python
import json
import pandas as pd

# 读CSV
df = pd.read_csv('experiments/watermark_test/method_cold/bert/ml-1m/0.1_1.0_5_100/logs/evaluation_results_summary.csv')
print(df[['attack_type', 'Recall@1', 'NDCG@10']])

# 读JSON
with open('experiments/watermark_test/method_cold/bert/ml-1m/0.1_1.0_5_100/logs/evaluation_results.json') as f:
    results = json.load(f)
    print(results)
```

---

## 对比分析建议

### 1️⃣ Data-Aware vs Data-Unaware

```bash
# 对比A1和A2的ASR差异
cat experiments/watermark_test/method_cold/bert/ml-1m/0.1_1.0_5_100/logs/*data*.csv
cat experiments/watermark_test/method_cold/bert/ml-1m/0.1_1.0_5_100/logs/*qee*.csv
```

### 2️⃣ COLD vs POP

```bash
# 对比A1(cold)和A3(pop)
# COLD的ASR应显著大于POP (≥3倍)
```

### 3️⃣ Soft-PRF vs Point-Level

```bash
# 对比A1和A1'的曲线
# Point-Level通常更激进，更接近完全替换
```

### 4️⃣ Beta消融曲线

```python
import matplotlib.pyplot as plt

betas = [1, 2, 5, 10, 15, 20]
asrs = [...]  # 从各个结果读取
ndcgs = [...]

plt.figure(figsize=(10, 5))
plt.subplot(1, 2, 1)
plt.plot(betas, asrs, 'o-')
plt.xlabel('Beta')
plt.ylabel('ASR (%)')
plt.title('ASR vs Beta')

plt.subplot(1, 2, 2)
plt.plot(betas, ndcgs, 'o-')
plt.xlabel('Beta')
plt.ylabel('ΔNDCG@10 (%)')
plt.title('质量损失 vs Beta')

plt.tight_layout()
plt.savefig('beta_tradeoff.png')
plt.show()
```

---

## 故障排查

| 问题 | 原因 | 解决 |
|-----|-----|------|
| HR@1 = 0 | 水印序列不匹配或损坏 | 检查 gen_watermark_seq.py 输出 |
| ΔNDCG > 5% | Beta过大或gamma不当 | 减小Beta或增大Gamma |
| QEE频率全0 | query_topk过大或数据稀疏 | 增加--freq_query_uniform_mix |
| 命令超时 | 数据集过大，模型推理慢 | 增加超时时间或使用--num_batches |
| 内存溢出 | batch过大 | 减小batch size或使用梯度检查点 |

